{
  "name": "@bluff-tavern/server",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bluff-tavern/shared": "workspace:*",
    "@fastify/cors": "^11.1.0",
    "fastify": "^5.5.0",
    "pino": "^9.9.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1",
    "tsx": "^4.20.5"
  }
}
