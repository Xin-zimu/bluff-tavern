# 《诡牌酒馆 / Bluff Tavern》V7.1 多模式扩展完整开发计划

> 基线版本：V7.0.0  
> 目标版本：V7.1.0  
> 版本主题：**Tavern Modes / 酒馆多模式扩展**  
> 核心原则：**保留 V7.0 已完成的道具、酒馆事件和轻量角色能力，但 V7.1 主线转向更多玩法模式；不破坏经典核心循环，用“每局只增加一条核心规则”的方式扩展玩法。**  
> 适用对象：DeepSeek / Codex / Claude Code 等开发 Agent，可直接作为实现规范与验收依据。

---

# 0. 文档目的

V7.0 已经完成《诡牌酒馆》的核心多人诈唬循环与第一批扩展玩法基础：

- 创建 / 加入房间；
- 2–8 人对局；
- A / K / Q / Joker 卡牌体系；
- 每轮随机目标牌；
- 每次出 1–3 张牌；
- 下一位玩家可继续出牌或质疑；
- 质疑后翻牌；
- 判定真假；
- 左轮惩罚；
- 淘汰；
- 最后一人生存获胜；
- 完整动画与房间生命周期；
- 重连恢复；
- Classic / Quick 两种模式；
- 道具、酒馆事件、角色能力三类扩展开关；
- 低风险道具、酒馆事件与轻量角色能力原型。

V7.1 不继续沿着“堆更多角色技能”的高学习成本路线扩张，而是优先增加**低学习成本、高差异度、适合朋友聚会**的游戏模式。

V7.1 的目标不是把游戏变复杂，而是：

> **在玩家已经会玩 Classic 的前提下，只需要再记住一句新增规则，就能立即理解一个新模式。**

---

# 1. V7.1 总体设计原则

## 1.1 一局只启用一个模式

V7.1 不允许自由组合多个特殊规则。

错误示例：

```text
Party + Free Challenge + Shared Revolver + Escalation
```

V7.1 必须采用：

```text
Classic
Quick
Party
Free Challenge
Shared Revolver
Escalation
```

六选一。

说明：这里的“六选一”指 V7.1 新增的房间玩法模式本身不互相叠加。V7.0 已落地的道具、酒馆事件、角色能力开关继续保留，并默认关闭。

原因：

1. 避免玩家一次记住过多规则；
2. 避免模式组合产生规则冲突；
3. 避免测试组合数量爆炸；
4. 保证新模式仍然建立在 V6 已验证稳定的核心状态机上；
5. 为后续 V7.2 Custom Rules 留出明确发展空间。

---

## 1.2 V7.1 不继续扩展角色能力

V7.0 已经完成一批轻量角色能力，并且它们必须继续保留为可关闭扩展。

以下内容明确不进入 V7.1 主线：

- 新增复杂主动技能；
- 新增复杂被动技能；
- 技能冷却；
- 技能次数；
- 技能相互克制；
- 技能连锁；
- 技能响应窗口；
- 角色强度平衡；
- 技能资源系统；
- 技能专属复杂 UI。

V7.1 中，角色能力只作为 V7.0 已有模块保留，不继续抢占多模式开发优先级。角色仍首先作为：

```text
视觉身份 + 表情演出 + 玩家形象
```

而不是 V7.1 的主要战斗数值扩展方向。

---

## 1.3 所有模式继续保持服务器权威

任何模式规则必须由服务器判断。

客户端不得自行决定：

- 当前玩家是否合法；
- 是否允许质疑；
- 谁抢到全民质疑；
- 本轮事件是什么；
- 是否允许出指定数量的牌；
- 共用左轮当前膛位；
- 惩罚是否命中；
- 玩家是否被淘汰；
- 谁是赢家。

客户端只负责：

```text
显示状态
发送意图
播放动画
```

---

# 2. V7.1 模式总览

| 模式 | 中文名 | 核心变化 | 学习成本 | 目标体验 |
|---|---|---|---|---|
| CLASSIC | 经典模式 | V6 标准规则 | 极低 | 原汁原味 |
| QUICK | 快速模式 | 7 秒出牌 | 极低 | 快节奏 |
| PARTY | 酒馆乱斗 | 每轮随机一个酒馆事件 | 低 | 聚会、随机、节目效果 |
| FREE_CHALLENGE | 全民质疑 | 所有人均可抢先质疑 | 低 | 全员参与、吵起来 |
| SHARED_REVOLVER | 死亡左轮 | 全桌共用一把左轮 | 低 | 高压、风险累积 |
| ESCALATION | 加注模式 | 出牌数量不得低于上一手 | 低 | 压力递增、逼迫质疑 |

---

# 3. 所有模式共同基础规则

除非模式章节明确覆盖，否则以下规则对全部模式成立。

## 3.1 玩家人数

```text
最少：2 人
最多：8 人
```

只有存活玩家参与实际回合。

淘汰玩家：

- 继续留在房间；
- 可观看动画；
- 不可出牌；
- 不可质疑；
- 不可参与游戏行为；
- 后续可继续使用社交类功能。

---

## 3.2 基础卡牌

卡牌类型：

```text
A
K
Q
JOKER
```

每轮随机选取：

```text
A / K / Q
```

之一作为目标牌。

例如本轮：

```text
目标牌：K
```

那么：

```text
K      = 真牌
JOKER  = 真牌
A      = 假牌
Q      = 假牌
```

除 Party 特殊事件明确修改 Joker 规则外，Joker 始终视作当前目标牌。

---

## 3.3 手牌

默认每轮每名存活玩家：

```text
5 张
```

每次正常出牌：

```text
1–3 张
```

除特殊模式或 Party 事件明确限制外，不得出 0 张或超过 3 张。

---

## 3.4 基础质疑

默认情况下：

1. 玩家 A 出牌；
2. A 声称这些牌全部是当前目标牌；
3. 下一名存活玩家 B 获得回合；
4. B 可：
   - 继续出牌；
   - 质疑 A。

如果 B 质疑：

- 若 A 的出牌中存在至少 1 张假牌：
  - A 质疑失败；
  - A 接受惩罚。
- 若 A 出的全部都是真牌 / Joker：
  - B 质疑失败；
  - B 接受惩罚。

---

## 3.5 空手强制质疑

如果上一名玩家出完牌后：

```text
handCount = 0
```

那么下一位玩家不得继续出牌。

必须进入：

```text
mustChallenge = true
```

此时只允许：

```text
质疑上一手
```

若该玩家超时：

```text
服务器自动执行质疑
```

---

## 3.6 默认胜利条件

除后续积分模式外，V7.1 所有模式继续使用：

```text
仅剩 1 名存活玩家
```

即结束整局。

---

# 4. 模式一：Classic / 经典模式

## 4.1 定位

Classic 是全部玩法的标准基准。

推荐标签：

```text
新手推荐
```

所有新玩家第一次体验游戏应优先选择 Classic。

---

## 4.2 核心规则

```text
每轮 5 张牌
每次出 1–3 张
回合时间 15 秒
Joker 为万能牌
只能由下一位玩家质疑
每人拥有独立左轮
最后存活者获胜
```

---

## 4.3 左轮

每位玩家有自己独立的左轮状态：

```ts
interface RevolverState {
  chamberCount: 6;
  bulletPosition: number;
  currentChamber: number;
  shotsTaken: number;
}
```

每位玩家的枪独立推进。

例如：

```text
狼：已经打过 3 个空膛
狐狸：还没开过枪
```

两人的风险不共享。

---

## 4.4 Classic 默认保持纯净

Classic 模式默认关闭：

```text
随机事件
全民质疑
共用左轮
加注规则
角色技能
道具
```

若房主主动开启 V7.0 已有扩展开关，则视为 Classic + 扩展内容，不计入 V7.1 新模式验收。Classic 默认状态必须始终保持为最稳定、最纯净的模式。

---

# 5. 模式二：Quick / 快速模式

## 5.1 定位

Quick 是 Classic 的快速版本。

推荐标签：

```text
快速对局
```

---

## 5.2 唯一规则差异

Classic：

```text
15 秒
```

Quick：

```text
7 秒
```

除此之外：

```text
牌组
Joker
质疑
独立左轮
胜利条件
```

全部与 Classic 相同。

---

## 5.3 超时规则

普通回合超时：

```text
服务器自动从玩家当前手牌中随机选择 1 张合法牌出牌
```

强制质疑状态超时：

```text
服务器自动质疑上一手
```

Quick 不允许客户端本地决定超时行为。

---

# 6. 模式三：Party / 酒馆乱斗

# 6.1 模式定位

Party 是 V7.1 的重要后续模式。

推荐标签：

```text
聚会推荐
```

核心规则：

> **每轮开始时，服务器随机抽取一个“酒馆事件”。该事件只影响当前轮。**

下一轮重新抽取。

---

# 6.2 Party 基础流程

```text
ROUND_START
↓
服务器抽取 Tavern Event
↓
全屏事件动画
↓
显示事件名称
↓
显示一句话规则说明
↓
事件卡缩小到桌面 HUD
↓
正常发牌
↓
开始本轮
↓
本轮结束
↓
事件清除
↓
下一轮重新抽取
```

---

# 6.3 Party 第一版事件数量

V7.1 第一版只启用以下 6 个事件：

```text
BLACKOUT
DRUNKEN
RAPID_NIGHT
DOUBLE_DANGER
NO_JOKER
FORCED_BET
```

不要一次增加十几个事件。

目标：

```text
少而清楚
```

优先于：

```text
多而混乱
```

---

# 6.4 BLACKOUT / 漆黑之夜

## 一句话规则

> 本轮隐藏其他玩家的准确手牌数量。

---

## UI

自己：

```text
我的手牌：3
```

其他玩家：

```text
手牌：?
```

---

## 服务端

服务端仍然保存：

```text
真实 handCount
```

但在生成公开 GameSnapshot 时：

Party + BLACKOUT 模式下，对当前客户端：

```text
自己的 handCount = 实际值
其他玩家 handCount = 隐藏
```

推荐不要改后端真实状态，只改公开视图。

---

## 不隐藏内容

以下信息仍正常显示：

- 玩家是否存活；
- 玩家是否在线；
- 当前出牌者；
- 玩家是否淘汰；
- 上一手出了几张；
- 目标牌。

---

# 6.5 DRUNKEN / 醉酒之夜

## 一句话规则

> 本轮出牌方向反转。

---

## 正常顺序

```text
1 → 2 → 3 → 4 → 5
```

---

## 醉酒之夜

```text
1 → 5 → 4 → 3 → 2
```

---

## 规则要求

必须新增本轮：

```ts
turnDirection: 'CLOCKWISE' | 'COUNTERCLOCKWISE'
```

普通模式：

```text
CLOCKWISE
```

DRUNKEN：

```text
COUNTERCLOCKWISE
```

本轮所有：

```text
下一玩家
淘汰后继承回合
强制质疑
下一轮起手玩家
```

都必须基于同一方向算法计算。

禁止某些流程顺时针、某些流程逆时针。

---

# 6.6 RAPID_NIGHT / 急迫之夜

## 一句话规则

> 本轮所有玩家只有 5 秒行动时间。

---

## 回合时间

Party 正常轮：

```text
15 秒
```

RAPID_NIGHT：

```text
5 秒
```

---

## 超时

仍沿用：

普通出牌超时：

```text
随机自动出 1 张
```

强制质疑超时：

```text
自动质疑
```

---

## UI

事件出现后，倒计时样式应更明显：

```text
5
4
3
2
1
```

最后 3 秒可以：

- 加快音效；
- 增强 HUD 闪烁；
- 不改变服务器实际倒计时。

---

# 6.7 DOUBLE_DANGER / 双倍危机

## 一句话规则

> 本轮质疑失败者连续接受两次左轮判定。

---

## 正常惩罚

```text
失败者
↓
开 1 枪
```

---

## 双倍危机

```text
失败者
↓
第一枪
↓
如果命中：立即淘汰，第二枪取消
↓
如果未命中：继续第二枪
↓
如果第二枪命中：淘汰
↓
如果仍未命中：进入下一轮
```

---

## 关键规则

不能：

```text
第一枪命中后还播放第二枪
```

也不能：

```text
一次计算两枪后直接显示结果
```

必须保持电影化顺序：

```text
第一枪 → 结果 → 停顿 → 第二枪 → 结果
```

---

# 6.8 NO_JOKER / 禁忌小丑

## 一句话规则

> 本轮 Joker 不再是万能牌。

---

## 示例

目标：

```text
K
```

则：

```text
K = 真
A = 假
Q = 假
JOKER = 假
```

---

## UI

目标牌公布时必须额外显示：

```text
Joker 本轮失效
```

Joker 卡牌视觉可以：

- 降低亮度；
- 添加禁用标记；
- 但玩家仍然可以正常将 Joker 当普通牌打出。

重要：

> Joker 不是“不能出”，而是“出 Joker 会被判定为假牌”。

---

# 6.9 FORCED_BET / 强制豪赌

## 一句话规则

> 本轮每次出牌至少 2 张。

---

## 合法数量

```text
2
3
```

非法：

```text
1
```

---

## 手牌只剩 1 张怎么办

如果当前玩家：

```text
handCount = 1
```

则不能正常出牌。

处理规则：

```text
若存在上一手 → 强制质疑
若是本轮第一手 → 允许特殊例外出 1 张
```

这样避免首位玩家因手牌结构直接死锁。

---

# 6.10 Party 事件抽取原则

每轮只允许：

```text
1 个事件
```

V7.1 不允许：

```text
BLACKOUT + DOUBLE_DANGER
```

同时出现。

---

## 连续重复限制

推荐：

```text
同一事件不得连续 2 轮出现
```

服务器记录：

```ts
previousTavernEvent
```

抽取下一轮时排除该事件。

---

# 7. 模式四：Free Challenge / 全民质疑

# 7.1 模式定位

核心规则：

> **任何存活玩家都可以质疑刚刚完成的出牌。**

目的：

- 降低等待感；
- 提高所有人的持续参与度；
- 让朋友聚会更容易产生争论和互动。

---

# 7.2 新流程

Classic：

```text
A 出牌
↓
下一位 B
↓
B 决定出牌 / 质疑
```

Free Challenge：

```text
A 出牌
↓
进入 CHALLENGE_WINDOW
↓
所有其他存活玩家出现“质疑”按钮
↓
持续 3 秒
↓
若有人抢先质疑
    → 立刻锁定该玩家
    → 进入质疑动画
若无人质疑
    → 下一位玩家进入正常出牌回合
```

---

# 7.3 质疑窗口时间

推荐：

```text
3000 ms
```

允许配置为服务器常量：

```ts
FREE_CHALLENGE_WINDOW_MS = 3000
```

不要由客户端自己倒计时决定结束。

---

# 7.4 谁可以质疑

允许：

```text
所有存活玩家
```

但不包括：

```text
刚刚出牌的玩家本人
已淘汰玩家
旁观者
断线玩家
```

---

# 7.5 抢质疑判定

采用：

> **服务器先收到并成功验证的合法请求获胜。**

不要使用客户端时间戳比较。

错误方式：

```text
客户端说自己 12:00:00.001 点击
```

正确方式：

```text
服务器请求到达顺序 + 原子锁定
```

---

# 7.6 抢到后立即锁定

当第一个合法质疑被接受：

```text
challengeLocked = true
challengerId = X
```

之后其他人的：

```text
game:challenge
```

统一返回：

```text
CHALLENGE_ALREADY_TAKEN
```

---

# 7.7 同一局只允许一个质疑者

不能出现：

```text
A 出牌
B 质疑
C 也质疑
D 也质疑
```

然后多人同时受惩罚。

必须只有：

```text
一个 challenger
一个 challenged player
一个 punished player
```

---

# 7.8 无人质疑

3 秒结束后：

```text
进入下一位存活玩家 TURN
```

下一位玩家此时：

```text
只能正常出牌
```

因为质疑窗口已经结束。

不要再次允许下一位玩家在 TURN 阶段质疑同一手，否则相当于获得双重质疑机会。

---

# 7.9 空手规则

如果上一名玩家出完牌后：

```text
handCount = 0
```

则 Free Challenge 不能直接“无人质疑后进入下一手”。

必须：

```text
CHALLENGE_WINDOW
↓
若 3 秒无人主动质疑
↓
服务器自动选择下一位存活玩家执行强制质疑
```

保证 V6 的空手死锁保护仍然存在。

---

# 7.10 UI

出牌完成后，全桌中央显示：

```text
狼声称出了 K × 3

质疑窗口
3.0
2.9
2.8
...
```

所有合法玩家看到：

```text
[ 质疑！ ]
```

出牌者看到：

```text
等待其他玩家质疑……
```

---

# 7.11 抢质疑反馈

允许短提示：

```text
狐狸抢先质疑！
```

不推荐显示：

```text
狐狸 212ms
熊 229ms
猫 231ms
```

避免把游戏变成反应速度排行榜。

---

# 8. 模式五：Shared Revolver / 死亡左轮

# 8.1 模式定位

核心规则：

> **全桌共用同一把六膛左轮。**

Classic 的独立左轮被替换成：

```text
1 把共享左轮
```

---

# 8.2 共享状态

服务器只维护：

```ts
interface SharedRevolverState {
  chamberCount: 6;
  bulletPosition: number;
  currentChamber: number;
  shotsTaken: number;
}
```

不再为每个玩家分别初始化枪。

---

# 8.3 整局初始化

比赛开始时：

```text
随机 bulletPosition
currentChamber = 0
shotsTaken = 0
```

子弹位置整局固定。

---

# 8.4 惩罚

任何玩家质疑失败：

```text
使用当前共享膛位
↓
扣动扳机
↓
如果空膛
    currentChamber += 1
    shotsTaken += 1
↓
枪保留当前进度
↓
下一次惩罚继续使用同一把枪
```

---

# 8.5 命中后怎么办

如果命中：

```text
当前惩罚玩家淘汰
```

然后必须重置共享左轮：

```text
重新随机 bulletPosition
currentChamber = 0
shotsTaken = 0
```

原因：

如果命中后不重置：

```text
子弹已经打出
```

剩余膛位将不存在危险。

所以每次命中淘汰后：

> **重新装入一发子弹并重新旋转。**

UI 要明确表现：

```text
装填
旋转弹巢
新的危险开始
```

---

# 8.6 风险信息

不要直接显示：

```text
下一枪死亡概率 33.33%
```

推荐显示：

```text
连续空膛：3
剩余膛位：3
```

让玩家自己理解风险。

---

# 8.7 UI

桌面中央始终显示共享左轮状态：

```text
共享左轮
空膛记录：● ● ● ○ ○ ○
```

注意：

- 不显示真实子弹位置；
- 不显示未来哪一膛会命中；
- 只显示已经发生的历史。

---

# 8.8 关键心理体验

例如：

```text
第一枪：空
第二枪：空
第三枪：空
```

下一位玩家知道：

```text
剩余三膛内存在一发子弹
```

因此：

```text
质疑的心理成本提高
```

这正是该模式的核心。

---

# 9. 模式六：Escalation / 加注模式

# 9.1 模式定位

核心规则：

> **本次出牌数量不得低于上一手。**

目标：

- 让回合压力自然递增；
- 逼迫玩家更早质疑；
- 增强“大牌下注”的心理压力。

---

# 9.2 合法数量

假设上一手：

```text
1 张
```

当前可出：

```text
1 / 2 / 3
```

上一手：

```text
2 张
```

当前可出：

```text
2 / 3
```

上一手：

```text
3 张
```

当前可出：

```text
3
```

或者：

```text
质疑
```

---

# 9.3 为什么不是“必须严格增加”

禁止设计为：

```text
1 → 2 → 3 → 必须质疑
```

因为这会导致：

- 流程过于机械；
- 三手以后必然结束；
- 玩家决策空间太小。

正确设计：

```text
不得少于上一手
```

而不是：

```text
必须多于上一手
```

---

# 9.4 当前玩家手牌不足

例：

上一手：

```text
3 张
```

当前玩家只剩：

```text
2 张
```

此时无法满足最低数量。

规则：

```text
强制质疑
```

服务器：

```text
mustChallenge = true
```

---

# 9.5 第一手

每轮第一位玩家没有上一手，因此：

```text
允许 1–3 张
```

---

# 9.6 质疑后新一轮

发生质疑并完成惩罚后：

```text
新轮重新开始
minimumPlayCount = 1
```

上一轮加注值不得跨轮保留。

---

# 9.7 UI

当前玩家必须看到明确提示：

```text
本次至少出 2 张
```

卡牌选择阶段：

- 选 1 张：
  - 出牌按钮禁用；
  - 显示“至少选择 2 张”。
- 选 2 / 3 张：
  - 出牌按钮可用。

---

# 9.8 超时

如果有合法出牌：

服务器自动：

```text
随机选择 minimumPlayCount 张合法手牌
```

如果手牌不足：

```text
自动质疑
```

---

# 10. 房间模式选择 UI

房主等待房间页面新增：

```text
选择游戏模式
```

推荐布局：

```text
经典模式
新手推荐
标准规则

快速模式
快速对局
7 秒决策

酒馆乱斗
聚会推荐
每轮一个随机事件

全民质疑
所有人都可以抢先质疑

死亡左轮
全桌共用一把枪

加注模式
出牌数量只能越来越高或持平
```

---

# 10.1 规则预览

玩家点击模式卡片时：

```text
打开模式详情弹窗
```

详情必须包含：

```text
模式名称
一句话简介
核心规则
与 Classic 的区别
```

不要显示几千字规则。

完整版规则仍放在：

```text
规则面板
```

---

# 10.2 房主改模式

只有房主可修改。

修改模式后：

```text
所有玩家 READY 状态重置
```

避免有人在不了解新规则时被直接开局。

---

# 11. 类型系统修改建议

V6 当前类似：

```ts
type GameMode =
  | 'CLASSIC'
  | 'QUICK'
  | 'PARTY'
  | 'CUSTOM';
```

V7.1 推荐改为：

```ts
export type GameMode =
  | 'CLASSIC'
  | 'QUICK'
  | 'PARTY'
  | 'FREE_CHALLENGE'
  | 'SHARED_REVOLVER'
  | 'ESCALATION'
  | 'CUSTOM';
```

但：

```text
CUSTOM
```

在 V7.1 中继续显示为：

```text
暂未开放
```

或暂时不显示入口。

---

# 12. GameSnapshot 建议扩展

推荐新增：

```ts
interface GameSnapshot {
  gameMode: GameMode;

  tavernEvent: TavernEventType | null;

  turnDirection: 'CLOCKWISE' | 'COUNTERCLOCKWISE';

  challengeWindow: {
    active: boolean;
    startedAt: number;
    endsAt: number;
    challengerId: string | null;
  } | null;

  sharedRevolver: {
    chamberCount: number;
    currentChamber: number;
    shotsTaken: number;
  } | null;

  minimumPlayCount: number;
}
```

注意：

```text
bulletPosition
```

绝不能暴露到公开 Snapshot。

---

# 13. 推荐新增 GamePhase

Free Challenge 推荐加入：

```text
CHALLENGE_WINDOW
```

完整顺序：

```text
ROUND_START
TURN
CHALLENGE_WINDOW
CHALLENGE_CALLOUT
REVEAL
VERDICT
PUNISHMENT_INTRO
PUNISHMENT_TRIGGER
PUNISHMENT_RESULT
ROUND_END
```

其他模式仍可跳过：

```text
CHALLENGE_WINDOW
```

---

# 14. 服务端架构原则

不要在 GameService 中到处写：

```ts
if (gameMode === 'PARTY') ...
if (gameMode === 'FREE_CHALLENGE') ...
if (gameMode === 'SHARED_REVOLVER') ...
```

推荐建立模式规则层：

```ts
interface ModeRules {
  getTurnDuration(): number;
  getMinimumPlayCount(state): number;
  canChallenge(context): boolean;
  getNextPlayer(context): string;
  resolvePunishment(context): PunishmentPlan;
}
```

或至少建立：

```text
mode-rules.ts
```

集中处理模式差异。

核心状态机继续保持统一。

---

# 15. V7.1 明确禁止的实现方式

禁止：

1. 为每个模式复制一套完整 GameService；
2. 客户端自己决定规则合法性；
3. 为 Party 每个事件创建完全独立状态机；
4. 在 V7.1 多模式主线中继续堆新的复杂角色技能；
5. 允许任意模式组合；
6. 把 Shared Revolver 的真实子弹位置发到前端；
7. Free Challenge 使用客户端时间戳判抢；
8. Escalation 在新轮继承上一轮最低出牌数；
9. Party 一轮同时触发两个随机事件；
10. 模式变更后保留 READY 状态。

---

# 16. 动画与表现规范

V7.1 新模式不能只改规则，不改表现。

## Party

必须有：

```text
事件抽取
事件全屏展示
事件规则说明
事件 HUD
```

## Free Challenge

必须有：

```text
质疑窗口
全员质疑按钮
抢到质疑后的聚焦动画
```

## Shared Revolver

必须有：

```text
桌面共享左轮
连续空膛记录
命中后的重新装填 / 旋转
```

## Escalation

必须有：

```text
最低出牌数 HUD
选择不足时明确反馈
达到 3 张时强化压迫感
```

---

# 17. 音效建议

V7.1 可复用现有声音系统。

推荐新增：

```text
party_event_reveal
challenge_window_open
challenge_claimed
shared_revolver_spin
shared_revolver_reload
escalation_pressure
```

不要把音效设计成持续刺耳循环。

---

# 18. 模式说明文本

建议玩家第一次进入特殊模式时显示 3 秒规则卡。

## Party

```text
酒馆乱斗
每轮会出现一个随机酒馆事件。
看清规则，它只持续这一轮。
```

## Free Challenge

```text
全民质疑
任何存活玩家都能质疑刚刚的出牌。
最快被服务器确认的人获得质疑权。
```

## Shared Revolver

```text
死亡左轮
全桌共用一把左轮。
连续空膛越多，下一枪越危险。
```

## Escalation

```text
加注模式
你的出牌数量不能少于上一手。
跟不上时，只能质疑。
```

---

# 19. 测试计划

# 19.1 Classic 回归

必须保证：

- 2 人可正常开始；
- 8 人可正常开始；
- 每轮发 5 张；
- 每次最多出 3 张；
- Joker 正常万能；
- 只有下一位可质疑；
- 每人独立左轮；
- 淘汰正确；
- 最后一人获胜；
- 重连正确；
- 超时正确。

---

# 19.2 Quick

测试：

- 回合 7 秒；
- 自动出牌；
- 强制质疑自动执行；
- 不影响左轮和胜利规则。

---

# 19.3 Party

分别测试六个事件。

### BLACKOUT

- 自己可见 handCount；
- 其他人不可见；
- 服务端真实数量不丢失。

### DRUNKEN

- 顺序完全反转；
- 淘汰后下一人方向正确；
- 强制质疑方向正确。

### RAPID_NIGHT

- 本轮 5 秒；
- 下一轮恢复正常。

### DOUBLE_DANGER

- 第一枪空后继续第二枪；
- 第一枪命中后立即停止；
- 两枪都空时进入下一轮。

### NO_JOKER

- Joker 被判定为假；
- 下一轮恢复万能。

### FORCED_BET

- 1 张被拒绝；
- 2–3 张合法；
- 只剩 1 张且存在上一手时强制质疑。

---

# 19.4 Free Challenge

测试：

- 3 秒窗口；
- 所有合法玩家均可提交；
- 出牌者不可质疑自己；
- 淘汰者不可质疑；
- 多人并发只有一个成功；
- 后续请求返回 CHALLENGE_ALREADY_TAKEN；
- 无人质疑后进入下一玩家；
- 空手时无人质疑触发自动强制质疑；
- 重连时窗口状态恢复正确。

---

# 19.5 Shared Revolver

测试：

- 只存在一个共享枪状态；
- 空膛后全桌共享进度；
- 命中正确淘汰当前失败者；
- 命中后枪正确重新装填；
- bulletPosition 不进入公开状态；
- 断线重连后共享枪状态一致。

---

# 19.6 Escalation

测试：

- 第一手 1–3 张；
- 上一手 1 → 当前 1–3；
- 上一手 2 → 当前 2–3；
- 上一手 3 → 当前仅 3；
- 手牌不足最低数量 → mustChallenge；
- 新轮最低值重置；
- 自动出牌满足最低数量。

---

# 20. 并发与断线专项

必须重点测试：

## Free Challenge 并发

至少模拟：

```text
3–5 个客户端几乎同时质疑
```

结果必须唯一。

## Shared Revolver

所有客户端 Snapshot 必须显示：

```text
相同 currentChamber
相同 shotsTaken
```

## Party

重连后必须恢复：

```text
当前事件
事件 HUD
倒计时
当前方向
```

---

# 21. V7.1 验收标准

只有以下全部满足，才能标记：

```text
v7.1.0
```

## 功能

- Classic 正常；
- Quick 正常；
- Party 六事件全部正常；
- Free Challenge 正常；
- Shared Revolver 正常；
- Escalation 正常。

## 稳定

- 2–8 人均可游戏；
- 不产生死锁；
- 不产生重复质疑；
- 不产生重复惩罚；
- 不出现两个赢家；
- 不发生模式状态串线。

## UI

- 玩家始终知道当前模式；
- 特殊规则清楚可见；
- 当前合法操作清楚；
- 质疑窗口明显；
- 共享左轮状态明显；
- 加注最低数量明显；
- Party 当前事件明显。

## 重连

任何模式断线后重连：

```text
必须恢复到正确状态
```

---

# 22. 推荐开发顺序

```text
V7.0.0
↓
V7.1-A
扩展 GameMode + 模式选择 UI
↓
V7.1-B
Escalation
↓
V7.1-C
Shared Revolver
↓
V7.1-D
Free Challenge
↓
V7.1-E
Party 基础框架
↓
V7.1-F
Party 六事件
↓
V7.1-G
模式动画 / UI / 音效
↓
V7.1-H
多人并发测试
↓
V7.1-RC
完整回归
↓
V7.1.0
```

推荐先做 Escalation 和 Shared Revolver，因为：

- 状态改动较局部；
- 容易验证模式框架；
- 不需要先增加新的复杂 Phase。

Free Challenge 再引入：

```text
CHALLENGE_WINDOW
```

Party 最后做，因为它依赖模式框架成熟。

---

# 23. V7 后续路线

V7.1 完成后：

```text
V7.2 — Party Expansion
```

增加更多酒馆事件，但继续保持：

```text
一轮一个事件
```

---

```text
V7.3 — Custom Rules
```

开放自定义组合：

```text
全民质疑
共享左轮
随机事件
加注
Joker 规则
回合时间
手牌数量
```

这时才允许复杂组合。

---

```text
V7.4 — Score Match
```

增加非淘汰积分赛：

```text
固定轮数
质疑成功得分
诈唬成功得分
失败扣分
最后按积分排名
```

解决多人聚会中：

```text
过早淘汰后只能看
```

的问题。

---

```text
V7.5 — Character Ability Update
```

已有轻量角色能力最后再进入扩展与平衡。

并且仍建议：

```text
先每个角色 1 个简单能力
```

而不是立即制作：

```text
主动 + 被动 + 冷却 + 技能树
```

---

# 24. 最终版本定位

V7.1 的完整定位：

> **《诡牌酒馆》第一次真正从“一个核心玩法”扩展成“一个多人聚会诈唬游戏平台”。**

V6 解决：

```text
基础玩法是否完整、清楚、稳定
```

V7.1 解决：

```text
同一套核心机制能不能玩出不同风格
```

最终玩家进入房间后可以根据场景选择：

```text
第一次玩        → Classic
想快点          → Quick
朋友聚会        → Party
想全员参与      → Free Challenge
想玩心理高压    → Shared Revolver
想被迫互相逼牌  → Escalation
```

而不是要求玩家在默认模式里先学习复杂技能体系：

```text
8 个强化角色
复杂主动 / 被动技能
技能触发时机
技能克制关系
冷却与次数
```

这正是 V7.1 应该坚持的设计方向。

---

# 25. 最终开发原则

开发 Agent 执行本计划时必须始终遵守：

```text
模式增加变化
而不是增加理解负担

服务器决定规则
客户端负责表现

一局只启用一个特殊模式

Classic 永远保持纯净

不继续提前堆叠复杂角色技能

不牺牲 V6 已完成的房间生命周期、动画、重连与稳定性
```

V7.1 的标准不是“功能数量最多”，而是：

> **每一个模式都足够清楚、足够稳定，而且玩家玩第一局就能感受到明显不同。**
