# 《诡牌酒馆 / Bluff Tavern》V6 后续完整开发计划

> 基线版本：V6.3.0  
> 当前目标：在不引入 V7 道具、技能和复杂扩展玩法的前提下，把现有核心玩法完善为一个**完整、易懂、有节奏、有压迫感、具备成熟棋牌游戏交互闭环的版本**。  
> 推荐最终收束版本：**V6.6.0**  
> 核心关键词：**房间生命周期、桌面可读性、目标牌演出、当前回合聚焦、质疑翻牌、电影化节奏、完整结算、移动端适配、稳定性收尾**

---

# 0. V6 当前阶段总结

V6.0～V6.3 已经完成了核心玩法和一部分电影化表现，游戏已经从“能运行的多人网页游戏”进入“具备明显美术和演出风格的可玩版本”。

但是实际试玩 V6.3 后，可以确认目前最大问题已经不再是“缺功能”，而是：

1. **房间生命周期不完整**：进入房间后缺乏退出、返回大厅、重新配置模式等完整路径；一局结束后基本只有“再来一局”。
2. **牌桌信息可读性不足**：目标牌只以 `K / A` 文字形式出现，不直观、不醒目，新玩家难以理解“本轮到底要求出什么”。
3. **当前出牌者不够突出**：等待别人出牌时，桌面缺少全局视觉聚焦，玩家很难第一眼知道“现在轮到谁”。
4. **质疑流程缺少真正的揭晓感**：质疑后没有清晰的翻牌动作，玩家不知道对方实际出了什么牌，失去了诈唬游戏最关键的心理落差。
5. **演出节奏过快**：质疑、揭牌、判定、惩罚之间停顿不足，整个过程显得局促，没有压迫感。
6. **桌面空间感不足**：目前仍偏“网页 UI + 动画”，还没有完全形成“多人围坐在同一张牌桌前”的感觉。
7. **结算后的选择不足**：不能很好地支持换模式、调整房间、继续等待、退出等多人游戏常见行为。
8. **V6 与 V7 的功能边界需要重新明确**：V6 应专注于把基础玩法做到完整和成熟；道具、技能、特殊事件等扩展玩法统一放到 V7。

因此，V6 后续不再继续横向增加玩法，而是进行一次完整的体验收束。

---

# 1. V6 最终目标重新定义

## 1.1 V6 不再定义为“动画版本”

V6 的最终定义调整为：

> **完成一个具备成熟棋牌游戏级核心体验的《诡牌酒馆》基础版本。**

也就是说，玩家第一次进入游戏，不需要别人额外解释，也能理解：

- 当前房间状态是什么；
- 这一局要出什么牌；
- 现在轮到谁；
- 谁刚刚出了什么声明；
- 谁正在质疑谁；
- 被质疑的实际牌是什么；
- 谁输掉了本次判定；
- 惩罚为什么发生；
- 一局结束后还能做什么；
- 如何返回房间、换模式、退出或继续。

---

## 1.2 V6 最终体验目标

最终 V6 应实现以下体验：

### 新玩家可读

玩家第一次进入对局，不看说明书，也能通过动画与 UI 理解玩法。

### 状态一眼可见

玩家在任意时刻都可以迅速判断：

- 当前阶段；
- 当前玩家；
- 本轮目标牌；
- 桌面已有牌；
- 上一次行动；
- 自己当前可执行的操作。

### 关键事件有仪式感

以下行为不再只是普通 UI 状态更新，而应成为“事件”：

- 开局目标牌公布；
- 当前回合切换；
- 出牌；
- 发起质疑；
- 翻牌；
- 判定；
- 惩罚；
- 淘汰；
- 回合结束；
- 整局结束。

### 房间流程完整

完整形成：

```text
大厅
 ↓
创建 / 加入房间
 ↓
房间等待区
 ↓
准备 / 房主配置
 ↓
开始游戏
 ↓
对局
 ↓
回合结算
 ↓
整局结算
 ↓
返回房间等待区
 ↓
再来一局 / 修改模式 / 等待玩家 / 退出房间
```

---

# 2. V6 与 V7 的明确边界

## 2.1 V6 必须完成

V6 只做“基础规则体验的完整化”：

- 房间生命周期；
- 房间等待区；
- 游戏开始演出；
- 目标牌提示；
- 回合切换；
- 当前玩家聚焦；
- 出牌动画；
- 质疑动画；
- 翻牌动画；
- 真假判定；
- 惩罚动画；
- 淘汰表现；
- 完整结算页；
- 返回房间；
- 再来一局；
- 修改模式入口；
- 退出房间；
- 返回大厅；
- 移动端适配；
- 断线/重连；
- 演出节奏统一；
- UI 信息层级重构；
- 音效和基础反馈；
- 最终稳定性收尾。

---

## 2.2 V7 明确不进入 V6

以下功能全部延后至 V7：

- 道具系统；
- 角色技能；
- 被动能力；
- 酒馆随机事件；
- 特殊卡牌体系扩展；
- 高级房间规则；
- 大量自定义模式；
- 成就；
- 等级；
- 货币；
- 商店；
- 外观收集；
- 战绩系统扩展；
- 赛季；
- 排位；
- AI 玩家高级策略；
- 观战系统；
- 大厅社交系统；
- 公共匹配；
- 好友系统；
- 语音聊天；
- 复杂表情互动；
- 额外小游戏。

V6 只负责把现在这套核心玩法做到“完整、顺畅、有质感”。

---

# 3. 后续版本规划总览

建议 V6.3 之后拆为三个主要版本：

| 版本 | 主题 | 目标 |
|---|---|---|
| V6.4 | 牌桌体验重构 | 解决信息可读性、房间生命周期、目标牌、当前回合、质疑翻牌、动画节奏问题 |
| V6.5 | 完整对局体验与新手友好 | 完善房间配置、结算、教程、提示、音效、移动端、断线重连和视觉统一 |
| V6.6 | V6 最终稳定版 | 全面测试、性能优化、异常路径、动画细节、服务器稳定、正式收束 V6 |

推荐：

```text
v6.3.0  当前基线
  ↓
v6.4.0  Table Experience Rework
  ↓
v6.5.0  Complete Match Experience
  ↓
v6.6.0  Final V6 Stable
  ↓
V7.0    扩展玩法正式开始
```

---

# 4. V6.4 —— 牌桌体验重构

## 4.1 版本定位

V6.4 不增加新的规则玩法。

这一版只解决一个问题：

> **让玩家真正看懂牌桌上正在发生什么。**

这是 V6 后续最重要的一版。

---

# 5. V6.4-A：完整房间生命周期

## 5.1 当前问题

V6.3 中房间逻辑更像：

```text
进入房间
 ↓
开始游戏
 ↓
游戏结束
 ↓
再来一局
```

这对多人游戏是不完整的。

---

## 5.2 新的房间生命周期

统一房间状态：

```ts
LOBBY
ROOM_WAITING
ROOM_READY
MATCH_STARTING
MATCH_PLAYING
ROUND_RESOLVING
MATCH_ENDING
MATCH_RESULT
RETURNING_TO_ROOM
ROOM_CLOSED
```

### 推荐简化状态

如果当前架构不适合过多状态，可以采用：

```ts
type RoomPhase =
  | 'waiting'
  | 'starting'
  | 'playing'
  | 'result';
```

对局内部再维护：

```ts
type MatchPhase =
  | 'round_intro'
  | 'turn_waiting'
  | 'player_action'
  | 'challenge'
  | 'reveal'
  | 'verdict'
  | 'punishment'
  | 'round_end';
```

---

## 5.3 房间等待区

完成一个真正的 Room Lobby。

展示：

- 房间名；
- 房间号；
- 房主；
- 当前模式；
- 当前人数 / 最大人数；
- 所有玩家；
- 准备状态；
- 房主设置；
- 邀请信息；
- 开始按钮；
- 退出房间按钮。

### 房主可操作

- 修改模式；
- 修改基础规则；
- 开始游戏；
- 踢出玩家；
- 转移房主（可 V6.5）；
- 解散房间。

### 普通玩家可操作

- 准备；
- 取消准备；
- 退出房间。

---

## 5.4 对局中退出

在游戏菜单中加入：

```text
继续游戏
游戏规则
声音设置
返回大厅 / 退出房间
```

退出必须二次确认：

```text
确定退出当前房间？

退出后本局将继续进行，
你的位置将根据房间规则处理。

[取消] [退出房间]
```

### 服务端行为

普通玩家主动退出：

- 从房间连接列表移除；
- 对局中标记为 disconnected / left；
- 根据当前规则决定直接淘汰或保留短暂重连窗口；
- 向其他玩家广播状态变化。

房主退出：

优先级建议：

1. 自动将房主转移给最早进入且在线的玩家；
2. 如果没有其他在线玩家，则关闭房间。

V6 不建议“房主一退房间直接销毁”。

---

# 6. V6.4-B：目标牌开局演出

## 6.1 当前问题

目前目标牌仅显示：

```text
K
```

或者：

```text
A
```

问题：

- 过于像调试 UI；
- 新玩家不理解；
- 视觉权重不足；
- 没有形成“这一局围绕这张牌展开”的记忆。

---

## 6.2 新目标牌演出流程

每局开始时：

### Phase 1：桌面暂时锁定

时间建议：`300~500ms`

- 禁止玩家操作；
- 桌面轻微压暗；
- 环境音保留；
- UI 降低透明度。

### Phase 2：中央目标牌出现

时间建议：`400~600ms`

中央展示真实卡牌视觉：

```text
       本轮目标

      ┌──────┐
      │      │
      │  K   │
      │      │
      └──────┘

所有玩家本轮声明的牌
均视为 K
```

### Phase 3：停留

时间建议：`900~1400ms`

让玩家真正读完。

### Phase 4：目标牌缩小并飞向 HUD

时间建议：`600~900ms`

动画：

```text
中央大卡
 ↓ scale
 ↓ translate
左上角 / 桌面目标位
```

### Phase 5：锁定为 Round Target HUD

左上角展示：

```text
┌────────────┐
│ 本轮目标    │
│    [ K ]   │
└────────────┘
```

---

## 6.3 目标牌 HUD

不能只显示字母。

应包含：

- 卡牌缩略图；
- “本轮目标”文字；
- Hover / 点击展开说明；
- Joker 规则简述；
- 当前轮次。

移动端建议点击展开：

```text
本轮目标：K

所有玩家出牌时都必须声明为 K。
实际牌面可以不是 K。
其他玩家可以选择质疑。
```

---

# 7. V6.4-C：当前玩家全局聚焦系统

## 7.1 核心目标

玩家不能靠阅读一小行文字来判断当前回合。

必须做到：

> **一眼就知道现在轮到谁。**

---

## 7.2 桌面角色布局

推荐继续使用 2.5D，而不是直接升级完整 3D。

示例：

```text
             玩家 B
           [角色立绘]

玩家 A                     玩家 C
[角色]       牌桌          [角色]

             玩家 D
           [角色立绘]
```

根据玩家数量动态排布：

- 2 人；
- 3 人；
- 4 人；
- 5 人；
- 6 人；
- 7 人；
- 8 人。

---

## 7.3 当前玩家视觉表现

当前出牌玩家同时触发：

- 角色整体亮度提高；
- 角色 scale `1.03~1.08`；
- 身后出现柔和轮廓光；
- 名字牌增强；
- 其他玩家轻微降权；
- 桌面光线朝向当前玩家；
- 显示“出牌中”；
- 可选轻微 idle 动画；
- 可选角色向前 6~12px。

禁止过度闪烁。

---

## 7.4 Turn Banner

回合切换时增加短暂提示：

```text
轮到「赤狐」出牌
```

如果是自己：

```text
你的回合
请选择 1～3 张牌
```

建议时序：

```text
上一回合结束
  ↓ 300ms
角色聚焦切换
  ↓ 300ms
Turn Banner 出现
  ↓ 800ms
Banner 淡出
  ↓
开放操作
```

---

## 7.5 非自己回合

自己的手牌仍可查看，但操作区降权。

推荐显示：

```text
等待 赤狐 出牌…
```

而不是仅禁用按钮。

---

# 8. V6.4-D：出牌演出重构

## 8.1 出牌动作流程

玩家完成选择后：

```text
选择牌
 ↓
点击出牌
 ↓
手牌区域锁定
 ↓
卡牌从玩家方向移动到桌面中央
 ↓
保持背面
 ↓
显示声明
 ↓
进入下一状态
```

---

## 8.2 声明信息

例如：

```text
赤狐 声明：2 张 K
```

不要只显示：

```text
2 K
```

---

## 8.3 桌面牌堆

桌面中央需要一个明确的公共牌区。

显示：

- 当前桌面牌数量；
- 最近一次出牌数量；
- 背面牌堆；
- 最近声明；
- 当前可质疑对象。

桌面牌不能和背景混在一起。

---

# 9. V6.4-E：质疑流程完全重做

这是 V6.4 的重点功能之一。

---

## 9.1 质疑开始

玩家点击“质疑”之后：

立即：

- 锁定所有操作；
- 关闭其他交互；
- 记录 challenge request id；
- 服务端确认质疑合法性；
- 进入 challenge cinematic。

---

## 9.2 质疑演出流程

推荐完整时间：`4.5~7 秒`。

参考：

```text
T+0.0  玩家点击质疑
T+0.2  操作锁定
T+0.4  桌面压暗
T+0.6  质疑者角色前移
T+0.8  “赤狐质疑灰狼！”
T+1.8  文案停留结束
T+2.1  镜头焦点转向桌面牌
T+2.7  第一张牌翻开
T+3.4  停顿
T+3.8  第二张牌翻开
T+4.6  停顿
T+5.1  判定文字出现
T+6.0  进入惩罚阶段
```

具体时长最终应可配置。

---

# 10. V6.4-F：真实翻牌系统

## 10.1 当前问题

现在质疑之后玩家无法真正看到：

> 对方到底出了什么。

这削弱了诈唬游戏最核心的反馈。

---

## 10.2 翻牌必须展示真实牌面

例如玩家声称：

```text
2 张 K
```

实际：

```text
K + 7
```

质疑后必须逐张展示：

```text
[背面] → [K]

停顿

[背面] → [7]
```

然后显示：

```text
欺骗成立
```

或：

```text
质疑失败
```

---

## 10.3 翻牌方式

CSS 3D transform 即可：

```css
transform: rotateY(180deg);
```

不需要引入完整 3D 引擎。

要求：

- 卡牌背面；
- 卡牌正面；
- perspective；
- 正反面 backface-visibility；
- 翻转阴影；
- 翻牌音效；
- 手机端 GPU 性能测试。

---

## 10.4 多张牌逐张翻

不能多张同时瞬间翻开。

建议：

```text
第 1 张
↓
停顿
↓
第 2 张
↓
停顿
↓
第 3 张
```

单张间隔：

`450~750ms`

最多 3 张时总长度可接受。

---

# 11. V6.4-G：电影化时间轴系统

## 11.1 禁止继续散落大量 setTimeout

如果目前动画逻辑分布在大量组件中，例如：

```ts
setTimeout(...)
setTimeout(...)
setTimeout(...)
```

V6.4 应重构。

---

## 11.2 建立统一 Cinematic Timing 配置

建议：

```ts
export const CINEMATIC_TIMING = {
  roundIntro: {
    dim: 300,
    cardEnter: 500,
    hold: 1200,
    dock: 700,
  },

  turnTransition: {
    delay: 250,
    focus: 350,
    banner: 900,
  },

  challenge: {
    dim: 300,
    titleIn: 400,
    titleHold: 900,
    tableFocus: 500,
    beforeReveal: 500,
  },

  reveal: {
    flipDuration: 500,
    gap: 550,
    finalHold: 800,
  },

  verdict: {
    enter: 350,
    hold: 1100,
  },

  punishment: {
    preDelay: 600,
  },
};
```

---

## 11.3 统一 Timeline Controller

推荐实现：

```ts
runCinematic(sequence)
```

或：

```ts
await cinematic.challengeIntro();
await cinematic.revealCards(cards);
await cinematic.showVerdict(result);
await cinematic.startPunishment();
```

避免多组件互相猜动画什么时候结束。

---

# 12. V6.4-H：动画期间操作锁定

所有关键动画期间必须统一锁定操作。

```ts
interactionLocked = true
```

适用于：

- 开局；
- 回合切换；
- 出牌发送中；
- 质疑确认；
- 翻牌；
- 判定；
- 惩罚；
- 回合结束。

UI 必须体现锁定，而不是点击无反应。

---

# 13. V6.4-I：全局对局状态提示

建议桌面顶部或中央上方增加一个弱化状态条：

```text
第 3 轮 · 当前：赤狐出牌
```

在关键事件中状态自动切换：

```text
等待灰狼出牌
灰狼已出 2 张牌
等待其他玩家决定是否质疑
赤狐发起质疑
正在揭牌
质疑成功
正在执行惩罚
```

状态条不能盖过电影化大标题。

---

# 14. V6.4-J：结算与返回房间

## 14.1 当前问题

现在结算后只有“再来一局”会导致玩家被困在当前流程里。

---

## 14.2 新结算页面

比赛结束后展示：

```text
胜利者

[角色]
赤狐

本局结果
存活：1
质疑成功：3
质疑失败：1
```

基础操作：

```text
[再来一局]
[返回房间]
[退出房间]
```

房主额外：

```text
[修改模式]
```

---

## 14.3 再来一局

不是立刻强制开局。

推荐：

```text
结算
 ↓
房主点击再来一局
 ↓
所有人返回房间准备状态
 ↓
玩家确认准备
 ↓
重新开始
```

可保留快速模式：

```text
房主：直接再来一局
```

但不作为唯一入口。

---

# 15. V6.4 服务端改造

前端演出不能只靠前端猜测。

服务端必须成为唯一 authoritative state。

建议统一事件：

```text
room:state
room:player_joined
room:player_left
room:host_changed
room:settings_changed

match:starting
match:started
match:phase_changed

round:started
round:target
round:turn_changed

play:submitted
play:resolved

challenge:started
challenge:revealed
challenge:verdict

punishment:started
punishment:resolved

player:eliminated

round:ended
match:ended
room:return
```

---

# 16. V6.4 数据结构建议

## RoomState

```ts
interface RoomState {
  roomId: string;
  hostPlayerId: string;
  phase: 'waiting' | 'starting' | 'playing' | 'result';
  mode: string;
  players: RoomPlayer[];
  settings: RoomSettings;
  match?: MatchState;
}
```

## MatchState

```ts
interface MatchState {
  matchId: string;
  roundNumber: number;
  phase: MatchPhase;
  targetRank: CardRank;
  currentPlayerId: string;
  activePlayerIds: string[];
  tablePileCount: number;
  lastPlay?: PublicPlayState;
}
```

## PublicPlayState

注意不能泄漏真实牌。

```ts
interface PublicPlayState {
  playerId: string;
  declaredRank: CardRank;
  count: number;
  cardsHidden: true;
}
```

只有 challenge resolve 后服务端才广播真实牌面。

---

# 17. V6.4 验收标准

V6.4 不以“代码写完”为完成。

必须满足：

### 房间

- [ ] 玩家能从房间退出；
- [ ] 玩家能返回大厅；
- [ ] 游戏结束能返回原房间；
- [ ] 房主能重新开始；
- [ ] 房主退出后房间不会无故卡死；
- [ ] 房间成员状态正确同步。

### 目标牌

- [ ] 开局中央展示真实目标卡牌；
- [ ] 有清晰说明；
- [ ] 有缩小飞向 HUD 动画；
- [ ] HUD 始终可见；
- [ ] 手机端不会遮挡角色。

### 回合

- [ ] 当前玩家一眼可见；
- [ ] 自己回合有明确提醒；
- [ ] 非自己回合有等待提示；
- [ ] 回合切换无误。

### 质疑

- [ ] 质疑对象明确；
- [ ] 桌面进入电影化状态；
- [ ] 所有被质疑牌逐张翻开；
- [ ] 正确显示实际牌；
- [ ] 正确显示质疑成功/失败；
- [ ] 动画节奏有停顿，不再瞬间完成。

### 结算

- [ ] 支持再来一局；
- [ ] 支持返回房间；
- [ ] 支持退出；
- [ ] 支持房主修改模式入口。

---

# 18. V6.5 —— 完整对局体验与新手友好

V6.4 解决“能看懂”。

V6.5 解决：

> **第一次玩也能顺畅玩，并且整个流程有完整产品感。**

---

# 19. V6.5-A：新手引导

不建议做超长强制教程。

使用上下文提示。

首次进入：

```text
本轮目标是 K
你可以出任意牌，但必须声明它们是 K。
```

第一次轮到自己：

```text
选择 1～3 张手牌，然后点击出牌。
```

第一次有人出牌：

```text
你可以相信他，也可以质疑。
如果质疑成功，对方接受惩罚。
如果质疑失败，你接受惩罚。
```

第一次出现 Joker：

```text
Joker 可以视为任意目标牌。
```

提示必须可关闭。

---

# 20. V6.5-B：游戏规则面板

游戏中随时可以打开：

```text
游戏规则
```

内容：

1. 基础目标；
2. 出牌；
3. 声明；
4. 质疑；
5. Joker；
6. 惩罚；
7. 淘汰；
8. 胜利条件。

不要把规则写成长墙。

采用卡片/步骤形式。

---

# 21. V6.5-C：房间配置完善

房主可配置 V6 范围内的基础参数。

注意：不是 V7 高级玩法。

推荐只开放：

- 玩家人数上限；
- 基础模式；
- 动画速度；
- 是否允许中途加入等待；
- 是否允许断线重连；
- 重新开始规则。

高级自定义规则进入 V7。

---

# 22. V6.5-D：声音系统

增加基础声音层：

### 环境音

- 酒馆环境；
- 火炉；
- 轻微人声；
- 木桌背景音。

### UI 音效

- 按钮；
- 选择牌；
- 取消牌；
- 出牌；
- 卡牌落桌；
- 翻牌；
- 质疑；
- 判定；
- 淘汰；
- 胜利。

### 动态音量

质疑前：环境音稍降。

揭牌：突出纸牌翻转。

判定：短促低频。

惩罚：再进入独立演出音效。

---

# 23. V6.5-E：视觉层级统一

所有 UI 分为四层：

```text
Level 1：场景层
Level 2：牌桌交互层
Level 3：状态 HUD
Level 4：电影化 Overlay
```

不要让普通按钮和质疑动画处于同一视觉权重。

---

# 24. V6.5-F：2.5D 桌面增强

不做完整 3D。

通过：

- parallax；
- scale；
- blur；
- perspective；
- lighting overlay；
- foreground/background；
- 角色 idle；
- camera zoom；

制造空间感。

---

# 25. V6.5-G：角色状态动作

每个角色尽量对应已有 8 个表情状态：

```text
待机
怀疑
撒谎
惊讶
大笑
紧张
淘汰
胜利
```

推荐映射：

| 游戏状态 | 表情 |
|---|---|
| 等待 | 待机 |
| 自己出牌 | 撒谎 / 待机随机 |
| 发起质疑 | 怀疑 |
| 被质疑 | 紧张 |
| 揭晓失败 | 惊讶 |
| 质疑成功 | 大笑 |
| 淘汰 | 淘汰 |
| 获胜 | 胜利 |

不需要每次都切换，避免过于戏剧化。

---

# 26. V6.5-H：移动端专项适配

手机浏览器优先。

必须测试：

- 360×800；
- 390×844；
- 412×915；
- iPhone 刘海 / 灵动岛安全区；
- HarmonyOS 浏览器；
- Android Chrome；
- 微信内置浏览器如需要。

---

## 手机布局原则

### 牌桌

角色不应全部挤在屏幕中部。

允许：

- 顶部对手；
- 左右侧人物；
- 当前玩家底部；
- 桌面中心保留给卡牌。

### 手牌

底部扇形展开。

### 操作

按钮固定底部安全区域。

### 目标牌

左上角缩略显示。

### 当前回合

不要完全依赖顶部文字，要靠角色高亮。

---

# 27. V6.5-I：断线重连完善

用户刷新页面或手机网络切换后：

必须恢复：

- 房间；
- 玩家身份；
- 手牌；
- 当前阶段；
- 当前玩家；
- 目标牌；
- 桌面牌；
- 剩余玩家；
- 当前动画结果。

---

## 重连不能重播所有旧动画

例如玩家在 reveal 阶段重连。

直接同步到：

```text
当前牌已揭晓
```

而不是从 10 秒前重新播放整个质疑。

建议增加：

```ts
stateVersion
serverTimestamp
phaseStartedAt
```

---

# 28. V6.5-J：等待与异常状态

增加明确提示：

```text
等待服务器响应…
正在重新连接…
房主已离开，正在转移房主…
该玩家已断线…
正在恢复对局…
房间已关闭
```

禁止白屏和无响应。

---

# 29. V6.5 验收标准

- [ ] 新玩家可通过 UI 基本理解规则；
- [ ] 规则面板完整；
- [ ] 2～8 人布局均可正常显示；
- [ ] 手机端核心按钮不被遮挡；
- [ ] 翻牌在手机上流畅；
- [ ] 角色状态和当前回合一致；
- [ ] 有基础音效；
- [ ] 可关闭音效；
- [ ] 刷新可恢复对局；
- [ ] 短暂断网可以重新连接；
- [ ] 房主掉线不会立即摧毁房间；
- [ ] 所有异常都有用户可理解提示。

---

# 30. V6.6 —— V6 最终稳定版

V6.6 不再加入明显新功能。

目标：

> **把 V6 收束成可以长期部署给朋友玩的稳定版本。**

---

# 31. V6.6-A：完整测试矩阵

## 玩家人数

逐个测试：

```text
2
3
4
5
6
7
8
```

---

## 网络状态

测试：

- 正常 Wi-Fi；
- 手机流量；
- 200ms 延迟；
- 500ms 延迟；
- 5% 丢包；
- 短暂断网；
- 刷新；
- 页面切后台；
- WebSocket 断连；
- 服务端重启后的错误处理。

---

## 房间异常

测试：

- 房主退出；
- 房主刷新；
- 普通玩家退出；
- 对局中玩家掉线；
- 结算时掉线；
- 满房间加入；
- 重复加入；
- 同一身份多开；
- 房间不存在；
- 房间已关闭。

---

# 32. V6.6-B：状态一致性测试

重点检查：

- 重复出牌；
- 重复质疑；
- 连点按钮；
- request id 重复；
- 旧事件晚到；
- phase 已变化但客户端仍发送旧操作；
- 同一玩家多个 socket；
- 结算阶段重复触发。

所有关键操作服务端需要幂等保护。

---

# 33. V6.6-C：性能优化

## 前端

检查：

- React 重渲染；
- 角色图片尺寸；
- 卡牌纹理；
- 动画 GPU layer；
- backdrop-filter 使用；
- 大量 box-shadow；
- 移动端内存；
- 页面长时间运行内存泄漏。

---

## 图片

建议：

- WebP / AVIF；
- 合理分辨率；
- preload 关键资产；
- 非关键资产 lazy load。

关键资产：

```text
牌背
A/K/Joker
当前角色
质疑 UI
惩罚 UI
```

必须在进入对局前预加载。

---

# 34. V6.6-D：动画性能降级策略

低性能设备自动减少：

- blur；
- parallax；
- 粒子；
- shadow；
- camera movement。

但绝不能取消：

- 目标牌信息；
- 当前玩家提示；
- 翻牌；
- 判定结果。

功能信息优先于装饰。

---

# 35. V6.6-E：减少动画选项

设置：

```text
动画：标准 / 快速
```

标准：完整电影化节奏。

快速：缩短停顿约 30～40%。

不要加入“完全关闭关键动画”，因为翻牌等动画本身承担信息传达。

---

# 36. V6.6-F：UI 最终统一

统一：

- 字体层级；
- 标题字号；
- 按钮高度；
- 金属边框；
- 酒红；
- 古铜；
- 暖象牙；
- 阴影；
- 圆角；
- Hover；
- Disabled；
- Active；
- Danger。

杜绝页面间视觉风格漂移。

---

# 37. V6.6-G：日志与调试

服务端日志必须记录：

```text
roomId
matchId
roundId
playerId
action
requestId
stateVersion
error
```

例如：

```text
[match]
room=ABCD
match=42
round=3
player=p2
action=challenge
request=req_123
```

不要把完整私密手牌无必要写入普通生产日志。

---

# 38. V6.6-H：健康检查

保持：

```http
GET /health
```

建议增强：

```json
{
  "status": "ok",
  "version": "6.6.0",
  "uptime": 123456,
  "rooms": 2,
  "connections": 7
}
```

如不希望暴露服务器状态，则生产环境只保留必要字段。

---

# 39. V6.6-I：最终异常兜底

前端增加统一 Error Boundary。

出现不可恢复错误：

```text
对局出现异常

你的房间状态仍保存在服务器。
可以尝试重新连接。

[重新连接]
[返回大厅]
```

不要只出现浏览器错误页。

---

# 40. 最终 V6 用户完整流程

V6.6 最终应该形成：

```text
打开游戏
 ↓
进入大厅
 ↓
创建 / 加入房间
 ↓
等待其他玩家
 ↓
房主选择模式
 ↓
所有玩家准备
 ↓
开始游戏
 ↓
角色进入牌桌
 ↓
目标牌中央公布
 ↓
目标牌缩入 HUD
 ↓
突出当前玩家
 ↓
当前玩家选择并出牌
 ↓
卡牌背面落到桌面
 ↓
显示声明
 ↓
其他玩家选择是否质疑
 ↓
无人质疑 → 下一玩家

或者

有人质疑
 ↓
质疑动画
 ↓
镜头聚焦桌面
 ↓
逐张翻牌
 ↓
揭示真实牌面
 ↓
真假判定
 ↓
惩罚演出
 ↓
淘汰 / 存活
 ↓
继续下一回合
 ↓
最终只剩胜者
 ↓
胜利结算
 ↓
再来一局 / 返回房间 / 修改模式 / 退出
```

这个流程跑顺之后，V6 才算真正结束。

---

# 41. 推荐开发顺序

不要并行大范围改动。

推荐严格按以下顺序执行：

## 第一阶段：状态架构

1. 整理 RoomState；
2. 整理 MatchState；
3. 整理 MatchPhase；
4. 统一服务端状态；
5. 增加 stateVersion；
6. 整理 socket 事件。

---

## 第二阶段：房间生命周期

1. 退出房间；
2. 返回大厅；
3. 返回房间；
4. 再来一局；
5. 房主切换；
6. 房间等待区；
7. 模式设置入口。

---

## 第三阶段：桌面信息

1. 目标牌 HUD；
2. 开局目标牌动画；
3. 当前玩家聚焦；
4. Turn Banner；
5. 桌面公共牌区；
6. 声明显示；
7. 全局状态条。

---

## 第四阶段：质疑系统

1. challenge phase；
2. 操作锁；
3. 质疑者聚焦；
4. 桌面镜头；
5. 卡牌翻转；
6. 多张逐张揭晓；
7. verdict；
8. punishment 衔接。

---

## 第五阶段：时间轴系统

1. 移除散落 timing；
2. 建立 CINEMATIC_TIMING；
3. 建立 timeline controller；
4. 标准动画速度；
5. 快速动画速度。

---

## 第六阶段：移动端

1. 2～8 人布局；
2. 手牌区；
3. HUD 安全区；
4. Overlay；
5. 翻牌性能；
6. 横屏/竖屏异常。

---

## 第七阶段：新手友好

1. 上下文提示；
2. 规则页；
3. 空状态；
4. 等待状态；
5. 错误状态。

---

## 第八阶段：稳定性

1. 断线；
2. 重连；
3. 重复请求；
4. 房主掉线；
5. 多 socket；
6. 性能；
7. 资源加载。

---

# 42. Git / 版本管理建议

当前基线：

```text
v6.3.0
```

建议：

```text
codex/v6.4-table-experience
codex/v6.5-complete-match-experience
codex/v6.6-final-stability
```

正式标签：

```text
v6.4.0
v6.5.0
v6.6.0
```

---

# 43. 每版交付内容

每个大版本至少包含：

```text
源码
CHANGELOG.md
测试结果
已知问题
部署说明
截图
关键流程 GIF / 视频（可选）
```

---

# 44. 每版完成标准

## V6.4.0

核心关键词：

```text
ROOM LIFECYCLE
ROUND TARGET
TURN FOCUS
CHALLENGE
CARD REVEAL
TIMING
```

如果质疑仍然只显示牌背，则 V6.4 不算完成。

如果一局结束后仍然只能“再来一局”，V6.4 不算完成。

---

## V6.5.0

核心关键词：

```text
ONBOARDING
MOBILE
AUDIO
RECONNECT
ROOM SETTINGS
VISUAL CONSISTENCY
```

要求玩家第一次玩时不需要别人一直解释。

---

## V6.6.0

核心关键词：

```text
STABILITY
PERFORMANCE
EDGE CASES
FINAL POLISH
```

不再增加明显功能。

目标：

> 部署后可以稳定留在服务器上，朋友聚会时直接打开就玩。

---

# 45. V6 最终优先级

如果开发过程中时间不够，优先级必须严格如下：

## P0 —— 必须完成

- 房间退出 / 返回大厅；
- 结算返回房间；
- 目标牌大动画；
- 目标牌 HUD；
- 当前玩家强提示；
- 质疑翻牌；
- 真实牌面揭晓；
- 判定停顿；
- 质疑动画整体拉长；
- 断线重连；
- 手机端可正常操作。

## P1 —— 强烈建议

- 房主转移；
- 完整 Room Lobby；
- 新手提示；
- 游戏规则页；
- 音效；
- 2.5D camera focus；
- 快速动画模式。

## P2 —— 有余力再做

- 更复杂统计；
- 更多角色微动画；
- 更复杂环境动态；
- 更多牌桌装饰；
- 高级转场。

P2 不能阻塞 V6 完成。

---

# 46. 禁止事项

V6 后续开发中明确禁止：

1. 为了“看起来高级”直接全面改成 Three.js；
2. 大规模重写已经稳定的网络层但没有测试；
3. 动画和服务端状态互相等待形成死锁；
4. 在动画组件里保存 authoritative game state；
5. 前端自行推断真实牌；
6. 质疑结果未收到服务器确认就提前播放；
7. 所有逻辑都塞进一个 GamePage；
8. 为 V7 道具提前污染核心规则；
9. 为视觉效果牺牲移动端可用性；
10. 一次性同时修改房间、游戏规则、网络协议和全部 UI。

---

# 47. 推荐前端模块拆分

```text
features/
├─ lobby/
├─ room/
├─ match/
│  ├─ state/
│  ├─ table/
│  ├─ hand/
│  ├─ turn/
│  ├─ challenge/
│  ├─ reveal/
│  ├─ punishment/
│  └─ result/
├─ cinematic/
├─ audio/
├─ onboarding/
└─ reconnect/
```

核心组件建议：

```text
RoomLobby
GameTable
PlayerSeat
TurnFocus
RoundTargetIntro
RoundTargetHud
TablePile
PlayDeclaration
ChallengeOverlay
CardRevealSequence
VerdictOverlay
PunishmentSequence
MatchResult
GameMenu
ReconnectOverlay
```

---

# 48. 推荐状态原则

严格遵循：

```text
服务端：事实
客户端：展示
动画：解释事实
```

而不是：

```text
动画 = 游戏逻辑
```

例如：

错误做法：

```text
动画结束 → 客户端认为玩家受罚
```

正确做法：

```text
服务器确认玩家受罚
↓
客户端收到事件
↓
客户端播放惩罚动画
↓
动画结束
↓
展示服务器已经确定的新状态
```

---

# 49. V6 完成后的 V7 起点

V6.6 完成后，V7 才正式开始“内容扩展”。

推荐 V7 第一阶段再考虑：

```text
道具
酒馆事件
特殊模式
角色能力
高级房间规则
社交互动
```

此时 V6 核心状态机应保持稳定，不再为了每个新道具反复改底层。

---

# 50. 最终结论

V6.3 已经证明核心玩法和电影化方向是成立的。

现在最不应该做的事情，是立即进入 V7 继续增加新玩法。

当前正确路线是：

```text
V6.3
  ↓
修完整房间生命周期
  ↓
强化目标牌表达
  ↓
强化当前玩家表达
  ↓
重做质疑与真实翻牌
  ↓
拉开动画节奏
  ↓
形成真正的 2.5D 牌桌
  ↓
补齐结算 / 返回 / 重开
  ↓
补新手提示 / 声音 / 移动端
  ↓
补断线 / 稳定性 / 性能
  ↓
V6.6 Final
  ↓
进入 V7 扩展玩法
```

V6 最终要实现的不是“更多功能”，而是：

> **玩家进入房间以后，从开局、出牌、质疑、翻牌、惩罚、淘汰到结算，每一步都知道发生了什么；关键事件都有节奏和压迫感；整个流程没有死胡同；手机上也能顺畅操作。**

做到这一点以后，《诡牌酒馆》才真正从一个网页游戏原型，进入一个完整多人派对游戏的基础形态。
