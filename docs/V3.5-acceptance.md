# V3.5 验收报告

日期：2026-08-17  
状态：通过

## 范围

- 八个原创角色可在大厅唯一选择；角色/道具/表情名称与 `ART_ASSETS_PLAN.md` 的资源槽位一致。
- 每局每位玩家持有五个私有一次性道具；服务端验证库存并消耗，客户端不能伪造结果。
- 八个表情通过实时 Socket.IO 广播；所有输入均受共享 Zod 枚举约束。

## 验证

- `pnpm lint`、`pnpm typecheck`：通过。
- `pnpm test`：27 项通过，含道具一次性消耗、角色唯一性、八客户端同步/恢复和全牌局回归。
- `pnpm build`：通过。

## 已知边界

- V3.5 使用原创文字/CSS 占位内容；V4.0 将按美术计划逐步替换为完整视觉、动画和音效资源。
