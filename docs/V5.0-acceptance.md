# V5.0 最终验收报告

日期：2026-08-17  
状态：通过

## 最终能力

- 2–8 人 Classic、Grand Table、Party 与 Custom；服务端权威出牌、质疑、轮盘、淘汰、胜利、再来一局。
- 角色选择、私有道具、固定表情、随机事件、观战式淘汰状态、自动托管出牌与 session 恢复。
- 手机/平板/桌面响应式、横屏/全屏、PWA、网络状态、低性能降级、原创视觉/音效表现。
- Docker、Nginx、systemd、CI、健康检查、优雅退出、命令限流、无秘密环境模板与发布脚本。

## 验证

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`：通过。
- 27 项测试覆盖服务端状态机、2–8 人动态牌堆、四/六/八个真实 Socket.IO 客户端、重连、道具、PWA 资源。
- `pnpm --filter @bluff-tavern/server exec tsx scripts/live-reconnect-v1.0.ts`：通过 Vite Socket.IO 反代进行真实断线/恢复验证。
- ZIP 使用 `git archive` 生成并以 `unzip -t` 验证；不含 `.git`、node_modules、缓存、日志或真实密钥。

## 运维说明

生产环境需由部署者提供 HTTPS 证书、域名和网络策略；仓库不含任何真实服务器凭据。
