# V4.0 验收报告

日期：2026-08-17  
状态：通过

## 范围

- 使用原创 1672×941 暖色酒馆背景，无角色、无文字、无 Logo、无原游戏/第三方资产。
- 牌桌拥有动态烛光回合高亮、卡牌材质、桌面入场和状态演出；动画严格为客户端表现。
- 低性能设备、reduced-motion 与窄屏会自动降低动画/背景负担。

## 验证

- `pnpm lint`、`pnpm typecheck`、`pnpm test`：通过，27 项测试通过。
- `pnpm build`：通过；Web public 中的原创背景被复制进生产 `dist/assets/backgrounds/`。

## 已知边界

- 当前角色仍使用 CSS/文字头像槽位；后续可按 `ART_ASSETS_PLAN.md` 分批替换为正式原创角色表情图。
