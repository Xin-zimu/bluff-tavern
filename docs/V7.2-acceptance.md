# V7.2 Acceptance

Use this checklist before merging `codex/v7.2-party-expansion` into `main`.

- Version: `7.2.0`
- `/health` returns `version: "7.2.0"`.
- Service Worker cache key is `bluff-tavern-v7.2.0-party-expansion`.
- Party draws exactly one public event per round.
- Party event pool contains HIDDEN_BET, DRUNKEN, RAPID_NIGHT, DOUBLE_DANGER, NO_JOKER, FORCED_BET, ONE_CARD_ONLY, MATCH_BET, HEAVY_HAND, LAST_CALL.
- Party uses weighted random selection and avoids the most recent two event types when possible.
- `GameSnapshot.minimumPlayCount` and `GameSnapshot.maximumPlayCount` match the current mode/event.
- ONE_CARD_ONLY allows exactly 1 card, including timeout auto-play.
- MATCH_BET locks later turns to the first successful play count and resets next round.
- HEAVY_HAND requires at least 2 cards from the opening play.
- LAST_CALL starts at 15 seconds, then 12, 9, and 6 seconds after accepted plays.
- RAPID_NIGHT is 5 seconds in Party.
- HIDDEN_BET hides other players' `handCount`, `cardCount`, and pre-reveal `lastPlay.count`; cumulative `discardCount` stays hidden from every viewer for the entire round, including the play owner, reconnect, and REVEAL. REVEAL publishes only the challenged play count and cards while hand counts stay hidden until the next round.
- DOUBLE_DANGER publishes each punishment shot separately.
- NO_JOKER makes Joker false for challenge, WOLF, FOX, and PANDA logic.
- POCKET_WATCH extends the current Party-derived turn time by 7 seconds.
- Reconnect restores event, min/max play count, turn direction, phase, phaseEndsAt, and punishment state.
- Socket views expose the same Party event and min/max rule to all clients.
- Party intro appears during ROUND_START without changing REVEAL / VERDICT / PUNISHMENT DOM.
- Event HUD remains visible after ROUND_START.
- Reduce Motion / Low Power keep Party intro simple.
- Desktop 1920x1080 and mobile 390x844 / 430x932 do not overflow.
- Classic, Quick, Escalation, Shared Revolver, Free Challenge, and Party core flows pass.
- `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass.
