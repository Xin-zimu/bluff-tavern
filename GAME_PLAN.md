# 网页多人「骗子酒馆类」派对游戏——完整开发执行计划

> 文档用途：直接交给其他 AI / Codex / Harness 执行  
> 部署目标：阿里云 Linux 服务器  
> 客户端目标：手机浏览器优先，同时兼容桌面浏览器  
> 核心玩法：诈唬出牌 + 质疑 + 轮盘淘汰 + 多人房间  
> 目标人数：2–8 人，重点优化 4 / 6 / 8 人局  
> 最终目标：可公网稳定部署、可多人联机、可持续迭代的完整网页派对游戏  
> 说明：玩法可参考《Liar's Bar / 骗子酒馆》的“诈唬、质疑、风险惩罚”核心思路，但不得直接复制其受版权保护的角色模型、贴图、音乐、UI、美术、文本、Logo 等资源。所有视觉、名称、角色、音效与界面应自行设计或使用可商用素材。

---

# 0. 执行总原则

AI 必须按照本文档逐阶段执行，不允许一上来只做一个 Demo 后停止。

每一个大版本都必须满足：

1. 可以独立运行。
2. 完成对应阶段的验收标准。
3. 修复当前版本已知严重 Bug。
4. 保留前一个版本的已有功能。
5. 输出完整源码。
6. 更新 `CHANGELOG.md`。
7. 更新 `README.md`。
8. 执行自动化测试。
9. 执行基本多人联机测试。
10. 生成该大版本 ZIP 包。
11. 不得使用“假按钮”“占位功能”“只有界面没有逻辑”的方式冒充完成功能。
12. 任何关键游戏判定必须由服务器完成，客户端不得拥有最终裁决权。
13. 所有多人状态必须考虑断线、刷新、重复请求、延迟和异常客户端。

开发顺序采用：

```text
玩法正确
→ 联机可靠
→ 手机可用
→ UI 完整
→ 动画与音效
→ 多模式
→ 性能、安全、部署
→ 最终打磨
```

禁止先花大量时间做美术，而核心联机仍不稳定。

---

# 1. 项目定位

项目暂定名：

**Bluff Tavern / 诡牌酒馆**

允许后期修改名称。

游戏定位：

- 手机浏览器直接打开即可玩；
- 无需安装 App；
- 一人创建房间；
- 分享 6 位房间码；
- 其他玩家加入；
- 2–8 人联机；
- 一局 5–20 分钟；
- 强调朋友聚会、心理博弈、互相欺骗、节目效果；
- 不做高频动作同步；
- 服务器只负责规则、房间和状态同步；
- 画面在客户端渲染；
- 阿里云不需要 GPU。

---

# 2. 最终产品体验

理想流程：

```text
玩家打开网页
    ↓
输入昵称 / 选择头像
    ↓
创建房间 或 输入房间码
    ↓
进入酒馆大厅
    ↓
其他玩家加入
    ↓
房主设置模式和人数
    ↓
全员准备
    ↓
开始游戏
    ↓
诈唬出牌 / 质疑
    ↓
失败者接受轮盘惩罚
    ↓
继续牌局
    ↓
最后存活玩家获胜
    ↓
战绩 / 再来一局
```

手机端必须支持：

- 竖屏进入；
- 自动提示横屏；
- 横屏完整游玩；
- 全屏模式；
- 触摸操作；
- 安全区适配；
- iPhone 刘海 / 灵动岛；
- Android 常见比例；
- 浏览器刷新后重连；
- 切后台后重新恢复；
- 低端手机降低动画质量。

---

# 3. 推荐技术栈

## 3.1 前端

推荐：

```text
React
TypeScript
Vite
Zustand
Socket.IO Client
PixiJS 或 Phaser
CSS / Tailwind CSS
Howler.js
```

说明：

- React：UI、房间、菜单、设置。
- PixiJS / Phaser：桌面、卡牌、角色、动画。
- 不建议第一阶段使用重型 Three.js。
- 后期如果确有必要，可为桌面增加轻量 3D 效果。
- UI 与游戏渲染层分离。

推荐优先：

**React + TypeScript + PixiJS**

---

## 3.2 后端

推荐：

```text
Node.js
TypeScript
Fastify 或 Express
Socket.IO
Zod
Pino
```

后期可加入：

```text
Redis
SQLite / PostgreSQL
```

第一版不要为了“架构高级”而过度复杂化。

---

## 3.3 工程工具

```text
pnpm
ESLint
Prettier
Vitest
Playwright
Git
Docker（后期）
Nginx
PM2 或 systemd
```

---

# 4. 推荐目录结构

```text
bluff-tavern/
│
├── apps/
│   ├── web/
│   │   ├── src/
│   │   │   ├── components/
│   │   │   ├── screens/
│   │   │   ├── game/
│   │   │   ├── stores/
│   │   │   ├── socket/
│   │   │   ├── audio/
│   │   │   ├── assets/
│   │   │   └── utils/
│   │   └── tests/
│   │
│   └── server/
│       ├── src/
│       │   ├── rooms/
│       │   ├── game/
│       │   ├── socket/
│       │   ├── auth/
│       │   ├── persistence/
│       │   ├── config/
│       │   └── utils/
│       └── tests/
│
├── packages/
│   ├── shared/
│   │   ├── protocol/
│   │   ├── types/
│   │   ├── rules/
│   │   └── constants/
│   │
│   └── ui/
│
├── docs/
│   ├── architecture.md
│   ├── protocol.md
│   ├── game-rules.md
│   ├── deployment.md
│   └── testing.md
│
├── deploy/
│   ├── nginx/
│   ├── systemd/
│   └── docker/
│
├── scripts/
│
├── README.md
├── CHANGELOG.md
├── SECURITY.md
├── pnpm-workspace.yaml
└── package.json
```

---

# 5. 核心架构原则

## 5.1 服务器权威

服务器必须拥有：

- 洗牌；
- 发牌；
- 手牌真实内容；
- 当前目标牌；
- 当前玩家；
- 出牌是否合法；
- 出牌真实内容；
- 质疑结果；
- 轮盘状态；
- 子弹位置；
- 玩家生死；
- 游戏胜负；
- 随机事件；
- 道具结果。

客户端只能发送“意图”：

```text
我要出第 1、3 张牌
我要质疑
我要准备
我要使用道具
```

客户端不得发送：

```text
我出了两张 Q
这次质疑成功
我没中弹
我赢了
```

---

# 6. 房间系统

## 6.1 房间属性

每个房间至少包含：

```ts
Room {
  id
  code
  hostPlayerId
  status
  mode
  maxPlayers
  players
  spectators
  settings
  gameState
  createdAt
  lastActiveAt
}
```

---

## 6.2 房间状态

```text
LOBBY
STARTING
PLAYING
ROUND_RESULT
GAME_OVER
CLOSED
```

---

## 6.3 玩家状态

```text
CONNECTED
DISCONNECTED
READY
PLAYING
ELIMINATED
SPECTATING
```

---

## 6.4 房间码

要求：

- 6 位；
- 字母数字；
- 排除易混淆字符；
- 例如排除：
  - O / 0
  - I / 1
  - L
- 房间关闭后回收；
- 必须检查碰撞。

示例：

```text
7K3M9X
```

---

# 7. 游戏核心规则

## 7.1 基础牌型

默认：

```text
A
K
Q
Joker
```

每轮服务器随机选择：

```text
A / K / Q
```

作为本轮目标牌。

Joker 视为万能牌。

---

# 8. 动态牌堆

不要将人数写死。

建议初始配置：

## 2–4 人

```text
A × 6
K × 6
Q × 6
Joker × 2
总计 20
```

## 5–6 人

```text
A × 9
K × 9
Q × 9
Joker × 3
总计 30
```

## 7–8 人

```text
A × 12
K × 12
Q × 12
Joker × 4
总计 40
```

目标：

```text
每人约 5 张牌
```

实际开发中允许根据测试结果调整。

所有数字必须放在配置文件中，不得硬编码散落在代码。

---

# 9. 出牌系统

玩家回合可以：

```text
选择 1–3 张手牌
↓
点击出牌
↓
客户端播放背面牌动画
↓
服务器记录真实牌
↓
其他玩家只能看到出牌数量
```

玩家口头声明在系统中可以简化为：

```text
“2 张 Q”
```

实际上目标牌由当前轮决定，因此 UI 可直接显示：

```text
玩家 X 声称出了 2 张 Q
```

---

# 10. 质疑系统

在规则允许的时机：

```text
上一名玩家出牌
↓
当前玩家
├─ 继续出牌
└─ 质疑
```

质疑后：

服务器翻开上一手牌。

如果其中存在不符合目标牌、且不是 Joker 的牌：

```text
上一名玩家撒谎
→ 上一名玩家失败
→ 进入惩罚
```

否则：

```text
质疑者判断错误
→ 质疑者失败
→ 进入惩罚
```

质疑期间：

- 禁止重复提交；
- 锁定游戏操作；
- 播放翻牌动画；
- 所有人收到同一个判定结果。

---

# 11. 轮盘惩罚系统

## 11.1 基础模式

不要把轮盘逻辑做成纯客户端动画。

服务器维护：

```ts
RevolverState {
  chamberCount
  bulletPositions
  currentChamber
  shotsTaken
}
```

服务器决定是否中弹。

客户端只显示结果。

---

## 11.2 经典模式建议

2–4 人：

```text
6 个弹巢
1 发实弹
```

---

## 11.3 大酒桌建议

5–6 人：

```text
6 个弹巢
1–2 发实弹
```

7–8 人：

```text
6 个弹巢
2 发实弹
```

---

## 11.4 可选渐进危险模式

可以增加一种独立规则：

```text
第一次：1/6
第二次：1/5
第三次：1/4
第四次：1/3
第五次：1/2
第六次：必定淘汰
```

但必须作为可配置模式，不能替代经典模式。

---

# 12. 玩家淘汰

玩家中弹后：

```text
alive = false
```

之后：

- 手牌移除；
- 从正常回合顺序跳过；
- 自动转为观战；
- 可以观看牌局；
- 不得发送出牌/质疑请求；
- 可以发受限表情。

最终：

```text
剩余 1 名存活玩家
→ GAME_OVER
```

---

# 13. 回合计时

为了防止 6–8 人局拖延：

默认：

```text
经典：15 秒
快速：7 秒
疯狂：5 秒
```

超时逻辑：

优先方案：

```text
如果允许继续出牌
→ 自动随机合法出 1 张
```

如果当前只允许质疑/继续：

```text
默认继续
```

计时必须以服务器时间为准。

客户端倒计时仅用于显示。

---

# 14. 多人模式

至少实现以下模式。

## 14.1 Classic

```text
2–4 人
标准牌堆
1 发实弹
标准时间
无道具
```

---

## 14.2 Grand Table

```text
5–6 人
扩展牌堆
节奏优化
更高轮盘压力
```

这是项目重点模式。

---

## 14.3 Party 8

```text
7–8 人
快速回合
扩展牌堆
随机酒馆事件
```

---

## 14.4 Custom

房主可设置：

- 最大人数；
- 回合时间；
- Joker 数量；
- 手牌数量；
- 子弹数量；
- 随机事件；
- 是否允许道具；
- 是否允许观战；
- 断线宽限时间。

---

# 15. 酒馆随机事件

仅 Party / Custom 模式启用。

建议首批：

## 停电

本轮降低视觉信息。

## 醉酒

手牌视觉顺序重新排列。

不得改变真实牌。

## 国王之夜

K 获得特殊规则。

## 女王之夜

Q 获得特殊规则。

## 双倍危机

下一次惩罚提高危险。

## 换座位

改变视觉座次或回合顺序。

## 快速夜

本轮计时减半。

所有随机事件必须：

- 服务器生成；
- 可复现；
- 有明确开始/结束；
- 不允许破坏游戏状态。

---

# 16. 道具模式

道具必须最后加入，不进入最早 MVP。

建议：

## 偷窥镜

查看随机一张指定玩家的牌。

## 换牌

和指定玩家随机交换一张牌。

## 封口

指定玩家本轮不能发起质疑。

## 酒杯

使目标玩家下一回合出牌数量受限制。

## 怀表

为自己增加回合时间。

## 烟雾

隐藏本次出牌数量直到进入下一玩家。

必须做平衡测试。

---

# 17. 手机 UI

核心原则：

```text
单手能点
按钮足够大
重要信息不被遮挡
不要依赖 hover
不要依赖右键
```

推荐主界面：

```text
┌──────────────────────────────────────┐
│        酒馆标题 / 房间 / 网络状态      │
│                                      │
│         对手座位区域                  │
│                                      │
│              桌面                    │
│        当前目标牌 / 出牌堆             │
│                                      │
│            你的手牌                  │
│                                      │
│   [取消选择]   [出牌]   [质疑]        │
└──────────────────────────────────────┘
```

---

# 18. 6 人布局

重点优化：

```text
             玩家4

      玩家5         玩家3


玩家6                   玩家2


              你
```

桌面在中央。

---

# 19. 8 人布局

建议：

```text
          P5     P4
      P6             P3
    P7                 P2
      P8             P1
             YOU
```

实际需根据屏幕比例动态计算。

所有座位使用：

```text
relative seat index
```

当前玩家永远显示在底部中央。

---

# 20. 卡牌交互

手机：

```text
点击卡牌
→ 卡牌上浮
→ 选中
```

允许最多 3 张。

点击：

```text
出牌
```

之后：

```text
卡牌飞向中央
```

禁止使用复杂拖拽作为唯一操作方式。

桌面端可额外支持：

```text
点击
拖拽
快捷键
```

---

# 21. 动画系统

第一阶段简单。

最终版至少包括：

- 发牌；
- 选牌；
- 出牌；
- 翻牌；
- 质疑；
- 镜头轻微推进；
- 轮盘；
- 开枪；
- 淘汰；
- 获胜；
- 房间加入；
- 随机事件；
- 表情。

动画不得阻塞服务器游戏逻辑。

服务器：

```text
判定完成
```

客户端：

```text
按事件队列播放动画
```

必要时：

```text
跳过动画
```

确保状态最终一致。

---

# 22. 角色系统

不要使用原作角色。

自建原创动物角色。

建议：

```text
狼
狐狸
兔子
熊
猫
青蛙
熊猫
浣熊
```

每个角色：

- idle；
- nervous；
- bluff；
- suspicious；
- shocked；
- laugh；
- shoot；
- eliminated；
- victory。

初期可以：

```text
静态立绘 + CSS/Pixi 动画
```

后期再升级骨骼动画。

---

# 23. 音频

包括：

- 酒馆环境声；
- 卡牌；
- 出牌；
- 翻牌；
- UI；
- 倒计时；
- 质疑；
- 轮盘；
- 空枪；
- 中弹；
- 胜利；
- 随机事件。

必须提供：

```text
总音量
音乐
音效
静音
```

手机浏览器需要在用户第一次交互后初始化音频。

---

# 24. 网络协议

统一事件命名。

客户端 → 服务端：

```text
room:create
room:join
room:leave
room:ready
room:updateSettings

game:start
game:playCards
game:challenge
game:useItem
game:emote

session:resume
ping
```

服务端 → 客户端：

```text
room:state
room:playerJoined
room:playerLeft

game:state
game:turnStarted
game:cardsPlayed
game:challengeStarted
game:challengeResult
game:punishmentStarted
game:punishmentResult
game:playerEliminated
game:eventStarted
game:over

error
pong
```

---

# 25. 协议统一格式

```ts
{
  eventId: string,
  roomId: string,
  playerId: string,
  timestamp: number,
  payload: {}
}
```

关键请求增加：

```text
requestId
```

防止重复提交。

---

# 26. 私有状态与公开状态

这是项目最重要的数据安全要求之一。

例如玩家 A 手牌：

服务器可以知道：

```text
Q
A
Joker
K
Q
```

玩家 A 收到：

```text
Q
A
Joker
K
Q
```

玩家 B 只能收到：

```text
cardCount = 5
```

绝对不能把其他玩家真实手牌发到浏览器，只依赖 UI 隐藏。

---

# 27. 断线重连

必须实现。

每个玩家获得：

```text
sessionToken
```

刷新页面：

```text
浏览器
→ session:resume
→ 服务端
→ 恢复房间
→ 恢复玩家
→ 下发当前完整可见状态
```

断线宽限：

```text
默认 60 秒
```

期间：

- 保留座位；
- 回合到该玩家时可自动托管；
- 重新连接恢复。

超过时间：

根据房主设置：

```text
托管
或
退出
```

---

# 28. 简单机器人托管

后期加入。

目的不是强 AI，而是：

- 玩家短暂断线；
- 不让房间卡死。

机器人：

```text
随机合法出牌
有一定概率质疑
遵守回合时间
```

不得读取不应知道的隐藏信息进行作弊。

---

# 29. 状态机

游戏必须显式使用状态机。

示例：

```text
WAITING
↓
DEALING
↓
TURN
├─ PLAYED
│   ↓
│ CHALLENGE_WINDOW
│   ├─ CONTINUE
│   └─ CHALLENGE
│        ↓
│      REVEAL
│        ↓
│     PUNISHMENT
│
└────────────────────
↓
ROUND_END
↓
NEXT_ROUND
↓
GAME_OVER
```

不得通过大量 if/else 隐式维持状态。

---

# 30. 随机数

服务器统一随机。

需要封装：

```ts
RandomService
```

用于：

- 洗牌；
- 目标牌；
- 子弹；
- 随机事件；
- 道具。

测试时允许注入固定 seed。

---

# 31. 防作弊

至少实现：

- 服务端权威；
- Zod 校验；
- 限流；
- requestId 去重；
- 非当前玩家不能出牌；
- 非法牌索引拒绝；
- 死亡玩家不能操作；
- 质疑窗口外不能质疑；
- 客户端不能指定结果；
- sessionToken 随机生成；
- 房间操作检查 host 权限；
- Socket 断开清理；
- 生产环境禁止暴露调试信息。

---

# 32. 日志

后端使用结构化日志。

至少记录：

```text
room_created
room_joined
game_started
turn_started
cards_played
challenge
punishment
player_disconnected
player_reconnected
game_finished
server_error
```

但日志不得记录：

- 密码；
- sessionToken；
- 全量隐私数据。

---

# 33. 统计系统

后期加入。

每局：

```text
胜者
人数
游戏时长
质疑次数
成功质疑
失败质疑
撒谎次数
最大连续幸存
```

玩家本地统计：

```text
总局数
胜率
成功质疑率
最常用角色
```

初期无需账户系统，可以使用匿名 player UUID。

---

# 34. 第一阶段：V0.1 工程骨架

目标：

建立可维护工程。

完成：

- Monorepo；
- React；
- TS；
- Node；
- Socket.IO；
- shared package；
- ESLint；
- Prettier；
- Vitest；
- 基础 README；
- `.env.example`；
- 健康检查；
- WebSocket 连接。

验收：

```text
pnpm install
pnpm dev
```

可以同时启动前后端。

浏览器显示：

```text
Server Connected
```

---

# 35. 第二阶段：V0.2 房间系统

完成：

- 创建房间；
- 房间码；
- 加入房间；
- 昵称；
- 玩家列表；
- 房主；
- 准备；
- 离开；
- 踢人；
- 最大人数；
- 房间配置。

验收：

至少打开 4 个浏览器窗口：

```text
A 创建
B C D 加入
```

四端状态一致。

---

# 36. 第三阶段：V0.3 核心牌局

完成：

- 洗牌；
- 发牌；
- 目标牌；
- 手牌；
- 轮到玩家；
- 1–3 张出牌；
- 继续回合；
- 牌堆耗尽；
- 新一轮。

先不要加入轮盘。

验收：

4 人可以完整打完多轮牌。

---

# 37. 第四阶段：V0.4 质疑

完成：

- 质疑窗口；
- 服务端翻牌；
- 判断真假；
- 失败者；
- 翻牌动画；
- 状态锁。

验收：

对以下情况写测试：

```text
真牌 + 质疑
假牌 + 质疑
Joker
多张牌部分为假
重复质疑
非当前玩家质疑
```

---

# 38. 第五阶段：V0.5 轮盘与淘汰

完成：

- RevolverState；
- 空枪；
- 实弹；
- 淘汰；
- 跳过死亡玩家；
- 剩余一人获胜；
- 游戏结束；
- 再来一局。

至此形成完整 MVP。

---

# 39. 第六阶段：V1.0 可玩 MVP

V1.0 必须达到：

```text
2–4 人
创建房间
联机
完整牌局
质疑
轮盘
淘汰
胜利
手机横屏
断线基本恢复
```

同时：

- 修复严重 Bug；
- 添加基础音效；
- 基础动画；
- 部署脚本；
- 测试。

V1.0 是第一个可以给朋友玩的版本。

---

# 40. 第七阶段：V1.5 手机体验

重点优化：

- 手机横屏；
- Safe Area；
- 大按钮；
- 卡牌触控；
- 全屏；
- PWA；
- reconnect；
- 手机后台恢复；
- 网络状态；
- 低电量/低性能动画；
- 防误触。

支持：

```text
iPhone Safari
Android Chrome
桌面 Chrome / Edge
```

---

# 41. 第八阶段：V2.0 6 人大酒桌

核心：

```text
支持 5–6 人
```

完成：

- 动态牌堆；
- 6 人座位；
- 回合节奏；
- 大人数轮盘；
- 快速模式；
- 人数配置；
- 6 人 UI。

必须专门测试：

```text
2
3
4
5
6
```

不同人数。

---

# 42. 第九阶段：V2.5 8 人模式

支持：

```text
7–8 人
```

完成：

- 8 人座位；
- 动态缩放；
- 快速回合；
- 玩家状态压缩；
- 8 人同步；
- 8 人断线重连。

必须执行至少一次真实 8 客户端压力测试。

可以使用：

```text
Playwright
```

模拟多个客户端。

---

# 43. 第十阶段：V3.0 Party 模式

完成：

- 随机酒馆事件；
- 事件 UI；
- 事件动画；
- 自定义模式；
- 多种轮盘规则；
- 房主配置。

目标：

明显区别于单纯模仿原玩法。

---

# 44. 第十一阶段：V3.5 道具与角色

加入：

- 道具；
- 角色选择；
- 原创角色；
- 表情；
- 动画；
- 音效。

至少：

```text
8 个角色
5 个道具
8 个表情
```

---

# 45. 第十二阶段：V4.0 完整视觉升级

重点：

- 酒馆背景；
- 动态灯光；
- 卡牌材质；
- 桌面；
- 角色动画；
- 质疑演出；
- 轮盘演出；
- 淘汰动画；
- 胜利动画；
- 过场。

保持：

```text
中端手机稳定运行
```

目标：

```text
60 FPS 优先
30 FPS 可接受降级
```

---

# 46. 第十三阶段：V4.5 社交与统计

完成：

- 对局记录；
- 本地统计；
- 房间聊天；
- 快捷表情；
- 玩家历史；
- 战绩结算；
- 分享结果卡片。

不允许开放无限制文本聊天导致安全和审核压力。

可以优先：

```text
快捷短语
表情
```

---

# 47. 第十四阶段：V5.0 最终版

V5.0 定义：

这是项目完整可长期使用版本。

必须完成：

## 游戏

- 2–8 人；
- Classic；
- Grand Table；
- Party；
- Custom；
- 完整质疑；
- 完整轮盘；
- 道具；
- 随机事件；
- 角色；
- 观战；
- 断线重连；
- 托管；
- 再来一局。

## UI

- 手机；
- 平板；
- 桌面；
- 横屏；
- PWA；
- 设置；
- 音效；
- 动画；
- 网络状态；
- 错误提示。

## 后端

- 房间；
- 状态机；
- 服务端权威；
- 防作弊；
- 日志；
- 限流；
- session；
- 自动清理房间；
- 健康检查；
- 优雅退出。

## 工程

- TypeScript 严格模式；
- ESLint；
- 测试；
- E2E；
- 文档；
- Docker；
- Nginx；
- CI；
- 部署脚本。

---

# 48. 测试计划

## 单元测试

至少覆盖：

```text
牌堆生成
洗牌
发牌
合法出牌
质疑
Joker
轮盘
淘汰
胜利判定
动态人数
回合顺序
断线
```

---

# 49. 网络测试

模拟：

```text
100ms
300ms
500ms
```

延迟。

测试：

- 重复点击；
- 数据乱序；
- 断线；
- 重连；
- 页面刷新；
- Socket 重建。

---

# 50. E2E

Playwright 自动：

```text
启动 4 个客户端
创建房间
加入
准备
开始
出牌
质疑
完成惩罚
继续游戏
```

后期增加：

```text
8 客户端
```

---

# 51. 压力目标

最低目标：

单服务器：

```text
100 个并发玩家
```

约：

```text
15–30 个房间
```

保持稳定。

由于游戏不是高频同步类型，这个目标应比较容易达到。

---

# 52. 阿里云部署

推荐：

```text
Ubuntu
Nginx
Node.js
PM2/systemd
HTTPS
```

结构：

```text
Internet
   ↓
443
   ↓
Nginx
   ├── /
   │   → Web 静态文件
   │
   └── /socket.io/
       → Node :3001
```

禁止直接将 Node 开发端口长期暴露公网。

---

# 53. HTTPS

必须启用 HTTPS。

可使用：

```text
Let's Encrypt
```

手机浏览器正式版本统一：

```text
https://game.example.com
```

WebSocket：

```text
wss://
```

---

# 54. systemd / PM2

生产服务要求：

- 开机启动；
- 崩溃自动重启；
- 日志；
- 优雅重启。

推荐生产最终版：

```text
systemd
```

---

# 55. Nginx

需要：

- 静态文件；
- WebSocket 反代；
- HTTPS；
- gzip / brotli；
- 缓存；
- 基础安全 header。

---

# 56. 安全组

公网只开放：

```text
80
443
22（尽量限制来源 IP）
```

不要开放：

```text
3000
3001
5173
```

给公网。

---

# 57. 与 Harness 共存

服务器可能同时运行：

```text
Harness
游戏服务
其他项目
```

要求：

- 使用独立 Linux 用户；
- 独立目录；
- 独立 systemd service；
- Harness 不直接以 root 长期运行；
- 游戏服务不拥有不必要的系统权限；
- 设置内存限制；
- 禁止 Harness 意外停止游戏服务。

推荐：

```text
/home/harness/
 /srv/bluff-tavern/
```

分离。

---

# 58. 备份

至少备份：

- `.env`；
- 配置；
- 数据库；
- 对局统计。

源码由 Git 保存。

不需要备份：

```text
node_modules
dist
缓存
```

---

# 59. CI

推荐 GitHub Actions：

```text
install
lint
typecheck
test
build
```

任何一步失败：

```text
禁止标记版本完成
```

---

# 60. 性能要求

前端：

```text
首次加载尽量 < 5 MB
```

核心游戏资源懒加载。

图片：

```text
WebP / AVIF
```

音频：

```text
压缩
按需加载
```

动画：

- 不创建大量 DOM；
- PixiJS 对象池；
- 卡牌复用；
- 避免频繁 React 重渲染。

---

# 61. 无障碍与可用性

至少：

- 颜色不是唯一状态提示；
- 质疑按钮有文字；
- 字体可读；
- 低对比场景提供增强模式；
- 支持关闭镜头晃动；
- 支持关闭闪光；
- 支持降低动画。

---

# 62. 错误处理

必须有用户可理解提示：

```text
房间不存在
房间已满
游戏已经开始
昵称无效
网络断开
正在重连
无法恢复会话
你不是房主
当前无法执行这个操作
```

不要直接显示：

```text
500
TypeError
undefined
```

---

# 63. AI 执行规范

交给 AI 后，应要求其：

1. 先阅读本计划。
2. 检查当前仓库状态。
3. 创建 TODO。
4. 只解决当前版本目标。
5. 每完成一个功能运行测试。
6. 不得因为 UI 未准备好就跳过后端逻辑。
7. 发现设计缺陷可以调整，但必须记录原因。
8. 修改规则必须同步更新 `game-rules.md`。
9. 修改协议必须同步更新 `protocol.md`。
10. 修改部署必须同步更新 `deployment.md`。
11. 每个大版本生成 Git tag 或版本号。
12. 每个大版本生成 ZIP。

---

# 64. AI 每阶段输出格式

每个阶段结束必须输出：

```text
版本：
完成内容：
新增文件：
修改文件：
测试结果：
已知问题：
下一阶段：
```

并在仓库写入：

```text
CHANGELOG.md
```

---

# 65. 禁止 AI 的行为

禁止：

- 只写 TODO 不实现；
- 大量使用 mock 假装完成；
- 客户端保存秘密手牌；
- 客户端决定游戏结果；
- 把 4 人逻辑硬编码后硬扩 8 人；
- 写死分辨率；
- 只兼容鼠标；
- 把游戏逻辑全部塞进一个文件；
- 使用未经许可的原作资源；
- 为了赶版本关闭测试；
- 把敏感密钥提交 Git；
- 开发服务直接裸奔公网。

---

# 66. 每个大版本的质量门槛

任何大版本发布前：

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

全部通过。

有 E2E 后：

```text
pnpm test:e2e
```

必须通过。

---

# 67. 最终交付内容

V5.0 ZIP 至少包含：

```text
源码
README
CHANGELOG
LICENSE
SECURITY
架构文档
协议文档
规则文档
部署文档
测试文档
Nginx 配置
systemd 配置
Docker 配置
环境变量模板
自动部署脚本
前端
后端
共享协议
测试
```

不得包含：

```text
真实 API Key
真实密码
真实服务器私钥
node_modules
临时缓存
```

---

# 68. 推荐版本路线总览

```text
V0.1
工程骨架
    ↓
V0.2
房间系统
    ↓
V0.3
核心牌局
    ↓
V0.4
质疑
    ↓
V0.5
轮盘与淘汰
    ↓
V1.0
完整 2–4 人 MVP
    ↓
V1.5
手机体验 + PWA + 重连
    ↓
V2.0
5–6 人大酒桌
    ↓
V2.5
7–8 人模式
    ↓
V3.0
Party + Custom + 随机事件
    ↓
V3.5
道具 + 角色
    ↓
V4.0
视觉 / 动画 / 音频全面升级
    ↓
V4.5
统计 / 社交 / 结算
    ↓
V5.0
安全、性能、部署、测试、最终打磨
```

---

# 69. V5.0 最终体验验收

最终必须能够完成下面这条完整流程：

```text
手机 A 打开网页
↓
创建 6 人房间
↓
生成房间码
↓
手机 B/C/D/E/F 输入房间码
↓
全部选择角色
↓
全部准备
↓
房主开始
↓
系统发牌
↓
玩家正常轮流诈唬出牌
↓
玩家进行质疑
↓
服务器判断真假
↓
失败玩家进入轮盘
↓
空枪 / 淘汰
↓
游戏继续
↓
有人断网
↓
重新打开网页恢复原座位
↓
继续游戏
↓
剩余一名玩家
↓
播放胜利动画
↓
显示数据统计
↓
所有玩家点击再来一局
↓
无需重新加入房间即可开始下一局
```

如果以上流程中任一关键环节无法稳定完成，则不能称为最终版。

---

# 70. 最终开发目标

该项目最终不是“复制一个骗子酒馆”。

真正目标是：

```text
一个以诈唬、质疑、风险惩罚为核心，
针对手机浏览器和 2–8 人朋友聚会重新设计，
拥有原创角色、原创酒馆美术、
多人数模式、随机事件、道具和完整联机系统的
网页多人派对游戏。
```

最终要求：

**打开网页就能玩，发一个房间码就能把朋友拉进来。**

这应成为整个项目所有技术决策和玩法设计的最高优先级。
